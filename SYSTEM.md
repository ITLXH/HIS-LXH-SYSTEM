# Luckxay Hospital HIS — System Reference

Single-source reference for the whole codebase. Read this BEFORE diving into `src/main.js` (12k lines) to save round-trips.

---

## 1. Stack

- **Frontend**: Vite + vanilla JS (jQuery + Bootstrap 5 + SweetAlert2 + Chart.js + DataTables)
- **Backend**: Supabase (PostgREST + RLS)
- **Auth**: Custom `HIS_One_Users` table (NOT Supabase Auth)
- **Hosting**: Cloudflare Pages (built `dist/`)
- **Languages**: Lao (default) + English i18n
- **Dev**: `npm run dev` on port 5176

## 2. File Map

```
index.html                          # SPA shell, loads main.js
src/main.js                         # ~12k lines — ALL logic
src/style.css                       # All styles
public/partials/navbar.html         # Top nav with role-based show/hide
public/partials/views/*.html        # Per-view templates (loaded by name)
public/partials/modals/             # Modal templates
public/partials/print-areas.html    # Print templates
supabase/migrations/*.sql           # Schema migrations (timestamp prefix)
supabase/restore_chunks/            # One-time data restore SQL (ignore)
.claude/launch.json                 # Preview server config
```

### Views (the only `loadView()` targets)

```
dashboard, report, visit_history, patients, triage, opd,
appointments, vaccines, vaccine_master, drugs, labs,
ipd_ward_bed, ipd_inpatient_list, ipd_chart, ipd_config,
services, locations, users, orgs, settings,
activity_log, backup, public-queue
```

Removed (do not reference): `ipd_dashboard`, `ipd_discharge`.

## 3. Database Schema (Supabase)

All tables prefixed `HIS_One_`. Use `dbTable('Name')` in JS.

### Core
| Table | PK | Notes |
|---|---|---|
| `HIS_One_Users` | `ID` (auto) | Login. Roles: `admin/doctor/nurse/lab/pharmacy/reception/cashier/staff` |
| `HIS_One_Patients` | `Patient_ID` (TEXT) | Format `LXH<YYYY>-<6digits>`, e.g. `LXH2026-002256` |
| `HIS_One_Visits` | `Visit_ID` | OPD visits |
| `HIS_One_OPD_Vital_Signs` | `Vital_ID` | Linked by `Visit_ID` + `Patient_ID` |
| `HIS_One_Appointments` | `Appt_ID` | |
| `HIS_One_Patient_Vaccines` | `Record_ID` | |
| `HIS_One_Organizations` | `Org_ID` | |
| `HIS_One_Vaccines_Master` | `Vac_ID` | |
| `HIS_One_Drugs_Master` | `Drug_ID` | |
| `HIS_One_Labs_Master` | `Lab_ID` | |
| `HIS_One_Service_Lists` | `ID` | |
| `HIS_One_Locations` | `ID` | District/Province lookup |
| `HIS_One_MasterData` | `ID` | Catch-all key/value (Category + Value) |
| `HIS_One_activity_logs` | auto | Audit trail |
| `HIS_One_Settings` | `Key` | Hospital name, logo, etc. |

### IPD Tables
| Table | PK | Linked by |
|---|---|---|
| `HIS_One_Admissions` | `Admission_ID` | `Patient_ID` |
| `HIS_One_Wards` | `Ward_ID` | |
| `HIS_One_Rooms` | `Room_ID` | `Ward_ID` |
| `HIS_One_Beds` | `Bed_ID` | `Ward_ID` + `Room_ID` |
| `HIS_One_Bed_Movements` | `Movement_ID` | `IPD_Admission_ID` |
| `HIS_One_IPD_Doctor_Notes` | `Note_ID` | `Admission_ID` |
| `HIS_One_IPD_Nursing_Notes` | `Note_ID` | `Admission_ID` |
| `HIS_One_IPD_Vital_Signs` | `Vital_ID` | `Admission_ID` |
| `HIS_One_IPD_Medication_Orders` | `Order_ID` | `Admission_ID` (legacy, hidden in UI) |
| `HIS_One_IPD_Radiology_Orders` | `Radiology_ID` | `Admission_ID` (legacy, hidden in UI) |
| `HIS_One_IPD_Procedures` | `Procedure_ID` | `Admission_ID` (legacy, hidden in UI) |
| `HIS_One_IPD_Billing_Items` | `Billing_ID` | `Admission_ID` (legacy, hidden in UI) |
| `HIS_One_IPD_Discharge_Summaries` | `Summary_ID` | `Admission_ID` |
| `HIS_One_IPD_Visits` | `Visit_ID` | `Admission_ID` (rounds, hidden in UI) |

### IPD Tables — Provider Tracking (added migration `20260620140000`)
All clinical IPD tables have:
- `Visit_ID` TEXT (link to round, optional now)
- `Provider_ID` TEXT (Users.ID)
- `Provider_Role` TEXT (`doctor`/`nurse`)

### Doctor Notes — Extra Fields (migration `20260620150000`)
- `Visit_Type` (Initial / Daily Round / Follow-up / Emergency)
- `Diagnosis`
- `Chief_Complaint`
- `Subjective`, `Objective`, `Assessment`, `Plan` (SOAP)

### Nursing Notes — Extra Fields (`20260620150000`)
- `Shift` (Morning/Evening/Night)
- `Patient_Condition`, `Observation`, `Nursing_Care_Given`
- `Response_To_Treatment`, `Intake`, `Output`, `Pain_Score`
- `Fall_Risk` (Low/Moderate/High)
- `Allergy_Alert`, `Medication_Given`, `Procedure_Done`
- `Notes` (free text)

### Admissions — Extra Fields (`20260621120000`)
- `Admitting_Nurse` TEXT (assistant nurse name, captured at quick admit)

### Vital Signs — Extra Fields (`20260620150000`)
- Standard: `Temperature`, `BP_Systolic`, `BP_Diastolic`, `Pulse`, `Respiration`, `SpO2`, `Weight`, `Height`, `BMI`, `Pain_Score`
- New: `Consciousness` (Alert/Verbal/Pain/Unresponsive/Drowsy/Confused)

## 4. Auth & Permissions

- Login: custom check against `HIS_One_Users.Email + Password` (no Supabase Auth)
- After login: `currentUser` global with `id, name, role, permissions, buttonPermissions`
- Role-based UI: navbar items have `.mnu-<view>` classes hidden then shown by perm
- `canUserAccessView(view, perms)` checks visibility
- `window.can(view, action)` checks button perms (view/edit/delete/etc.)

## 5. Modules & Key Functions

All entry points are `window.*` functions in `src/main.js`.

### Patients
- `loadPatients()`, `initPatientTable()`
- `editPatient(id)`, `viewPatientDetail(id)`, `delPatient(id)`
- `generateNextPatientID()` → returns `LXH<YYYY>-<6digit>`
- `handlePatientExcelUpload(e)` — Excel import using upsert (line 8027). On conflict by `Patient_ID`, updates existing.
- `normalizePatientCode(value)` — trim + uppercase

### OPD / Visits
- `loadQueue()`, `handleVisitEdit()`, `viewVisitDetail()`
- Visits + Vitals saved together
- LIS/Labs are JSON in `Visits.Lab_Orders_JSON`, prescriptions in `Prescription_JSON`

### Appointments
- `loadAppointments()`, `saveAppointment()`, `deleteAppointment()`
- Status flow: pending → Completed / Missed / Cancelled

### Vaccines
- Master: `loadVaccineMaster()`, `saveVaccine()`, `deleteVaccine()`
- Patient: `loadPatientVaccines()`, `addPatientVaccine()`, `deletePatientVaccine()`

### Drugs / Labs / Services / Locations / Orgs
- All follow same pattern: `load<X>Master()` + Add/Edit/Delete

### Users (Settings → Users)
- `loadUsers()`, `openAddUserModal()`, `openEditUserModal()`, `deleteUserRow()`
- Per-role default permissions set in `togglePermissionsBox()`

### MasterData
- Generic key/value store, used for lab categories, dropdown options
- `Category` + `Value` columns
- Helpers: insert/update/delete in settings flow

## 6. IPD Module (post-simplification)

### Navigation (2 items only)
1. **Ward / Bed Management** (`ipd_ward_bed`) — DEFAULT landing. Bed board grid. Top toolbar has the green **Admit Patient** button → `openIpdQuickAdmitModal()` (admit no longer has its own page).
2. **Inpatient List** (`ipd_inpatient_list`) — Flat table for search/print. Tabs: Active / Discharged / All (state in `window.ipdInpatientFilter`, default `'active'`). Discharged rows show "View History" only (no transfer/discharge buttons) and open the chart in read-only mode.

`ipd_chart` is a sub-view opened from the bed board (not in nav menu).

### IPD Chart — Single Timeline View
File: `public/partials/views/ipd_chart.html` (~25 lines).
- Header: subtitle (`IPD<id> · HN<id> · <patient name>`) + 6 action buttons:
  - 🔵 **ເພີ່ມການກວດຂອງໝໍ** (`openIpdDoctorNoteModal()`)
  - 🟢 **ເພີ່ມບັນທຶກພະຍາບານ** (`openIpdNursingNoteModal()`)
  - 🔴 **ເພີ່ມສັນຍານຊີບ** (`openIpdVitalModal()`)
  - 🟡 **Edit Discharge** (`openIpdDischargeSummaryModal()`)
  - **Print Discharge** (`printIpdDischargeSummary()`)
  - **Back to Ward Board** (`loadView('ipd_ward_bed')`)
- Filter pills: All / Doctor / Nursing / Vitals / Linked OPD
- Timeline EMR-style: date headers (Sat 20 Jun 2026 · N events), each row = time + icon + type badge + provider + body + CRUD buttons

### State
```js
window.ipdClinicalState = {
  admissionId, admission,
  visits: [],            // OPD visits (read-only context)
  doctorNotes: [],
  nursingNotes: [],
  vitals: [],
  medicationOrders: [],  // legacy, no UI
  radiology: [],         // legacy, no UI
  procedures: [],        // legacy, no UI
  billing: [],           // legacy, no UI
  dischargeSummary: null,
  rounds: [],            // legacy IPD_Visits, no UI
  providers: [],         // doctor/nurse cache
  movements: [],
  timelineFilter: 'all',
  timelineLimit: 200
};
window.ipdCurrentChartAdmissionId  // current open chart
window.ipdCurrentChartReadOnly     // true when viewing a discharged admission (history)
window.ipdInpatientFilter          // 'active' | 'discharged' | 'all' (Inpatient List tabs)
window.ipdActiveVisitId            // legacy, no UI
window.ipdProvidersCache           // [{id, name, role}, ...]
```

### Key IPD Functions
- `loadIpdClinicalChart(admissionId)` — fetch all + render
- `renderIpdChartPage(id)` — just subtitle + timeline
- `renderIpdTimeline()` — EMR-style grouped by date
- `buildIpdTimelineEvents()` — collects events from doctorNotes/nursingNotes/vitals/visits(OPD)/movements/discharge
- `ipdTimelineEvent({at, type, filter, title, body, meta, icon, id, entityType, provider, providerRole, readOnly})`
- `ipdTimelineEntityActions` map — `entityType` → `{open, table, idCol}` for CRUD wiring
- `ipdLoadProviders()` — cache doctor/nurse users
- `ipdProviderOptions(selectedId, roleFilter)` — `<option>` HTML
- `ipdCurrentProviderDefault()` — returns logged-in user as provider
- `ipdUpsertClinical(table, idCol, payload, existingId)` — generic upsert + refresh
- `ipdDeleteClinical(table, idCol, id)` — confirm + delete + refresh
- `ipdSelectClinical(table, admissionId, orderCol, asc)` — fetch
- `ipdNeedsMigration(error)` — checks `column does not exist` errors → returns []

### IPD Entry Modals
All accept optional `id` (= edit existing, else create). All write `Provider_ID`/`Provider_Role` + use translated values.

| Function | Roles allowed | Modal title |
|---|---|---|
| `openIpdDoctorNoteModal(noteId?)` | doctor | ການກວດຂອງໝໍ (SOAP) |
| `openIpdNursingNoteModal(noteId?)` | nurse | ບັນທຶກພະຍາບານ (ປະຈຳກະ) |
| `openIpdVitalModal(vitalId?)` | doctor + nurse | ບັນທຶກສັນຍານຊີບ |
| `openIpdDischargeSummaryModal()` | any | ສະຫຼຸບການຈຳໜ່າຍ |

## 7. i18n

- Stored in `window.appTranslations.{en,lo}` objects (multiple `Object.assign` blocks)
- Access: `window.t('key')` — returns current-language string or key if missing
- `window.ipdTranslateValue(value)` — looks up `option.<value>` keys for dropdown values
- Language switch: dropdown in navbar, persisted to localStorage
- Lao is default. Adding a key: append to BOTH `appTranslations.en` AND `appTranslations.lo` blocks.

## 8. Naming Conventions

- All custom helpers prefixed `window.ipd*` for IPD-related, `window.*` global
- Patient IDs: `LXH<YYYY>-<6digits>` (e.g. `LXH2026-002256`)
- IPD Admission IDs: `IPD<long unique>` (timestamp-based, generated by `ipdId('IPD')`)
- Other prefixes via `ipdId(prefix)`: `DN` (doctor note), `NN` (nursing), `VS` (vital), `MO` (med order), `RAD`, `PROC`, `BILL`, `DS`, `IPDV`
- All Supabase table refs go through `dbTable('Name')` (lets us swap prefix in dev)

## 9. Recent Migrations (chronological)

| Date | File | Purpose |
|---|---|---|
| 2026-06-16 | `20260616142931_remote_schema.sql` | Initial schema snapshot |
| 2026-06-18 | `20260618093000_ipd_ward_bed_management.sql` | Wards/Rooms/Beds/Admissions/Movements |
| 2026-06-19 | `20260619090000_ipd_clinical_chart.sql` | All IPD clinical tables (Doctor Notes, Vitals, Med, etc.) |
| 2026-06-19 | `20260619110000_ipd_bed_reservation_details.sql` | Reservation fields on beds |
| 2026-06-20 | `20260620130000_add_old_patient_id.sql` | `Old_Patient_ID` column |
| 2026-06-20 | `20260620140000_ipd_visits.sql` | `IPD_Visits` table + `Visit_ID/Provider_ID/Provider_Role` columns on all IPD action tables |
| 2026-06-20 | `20260620150000_ipd_simplify_emr.sql` | Doctor SOAP-Plus + Nursing rich fields + Vitals Consciousness |
| 2026-06-21 | `20260621120000_ipd_admitting_nurse.sql` | `Admitting_Nurse` TEXT column on `HIS_One_Admissions` (assistant nurse on quick admit) |
| 2026-06-23 | `20260623090000_opd_observations.sql` | `opd_observations` + `opd_observation_notes` tables + duration trigger |
| 2026-06-23 | `20260623120000_opd_observations_beds.sql` | `ward_id/room_id/bed_id` + `opd_active_observations_by_bed` view |
| 2026-06-24 | `20260624140000_opd_observation_notes_ipd_parity.sql` | IPD-style columns on `opd_observation_notes` (SOAP, shift, BMI, Consciousness, provider tracking) |

## 10. Ad-hoc SQL (in `supabase/` root)

- `fix_patient_id_padding.sql` — DO-block to renumber short Patient_IDs (e.g. `LXH2026-009` → next available 6-digit). Updates all 6 child tables in one transaction. Run after Excel imports that mixed short/long IDs.
- `insert_recovered_patients.sql` — INSERT for 21 patients lost when Excel had duplicate Patient_IDs.

## 11. Recent Behavioural Changes (cheat sheet)

| Date | What changed | Where |
|---|---|---|
| 2026-06-20 | Patients Excel upload: `insert` → `upsert(onConflict='Patient_ID')` + dedupe in JS + report counts | `handlePatientExcelUpload` |
| 2026-06-20 | Patient table sort: by ID desc using `data-order` numeric sort key | `initPatientTable` |
| 2026-06-20 | Patient ID generator: 4-digit no-dash → 6-digit with dash (`LXH2026-002256`) | `generateNextPatientID` |
| 2026-06-20 | IPD chart simplified: 12 tabs → 1 timeline | `ipd_chart.html` + `renderIpdChartPage` |
| 2026-06-20 | Patient header & snapshot bar removed from IPD chart (kept only subtitle) | `renderIpdChartPage` |
| 2026-06-20 | IPD nav: 5 items → 3 (removed Dashboard + Discharge views) | `navbar.html` |
| 2026-06-20 | Modal titles now include role icon + clear language ("ການກວດຂອງໝໍ", "ບັນທຶກພະຍາບານ") | 3 modals |
| 2026-06-21 | Inpatient List: filter tabs Active / Discharged / All + new Discharge Date column. Default = Active. Discharged rows show only "View History" action button. State: `window.ipdInpatientFilter` | `ipd_inpatient_list.html`, `renderIpdInpatientTable`, `loadIpdInpatientListPage`, `bindIpdInpatientFilterTabs`, `applyIpdInpatientFilterTabUi` |
| 2026-06-21 | `viewIpdChart()` no longer blocks discharged admissions. Sets `window.ipdCurrentChartReadOnly = true` and `renderIpdChartPage` injects `#ipdChartReadOnlyBanner` (alert-info banner) when read-only. | `viewIpdChart`, `renderIpdChartPage` |
| 2026-06-21 | Quick Admit modal rebuilt: patient search now rebuilds `<option>` list (was `.toggle()` which doesn't work on options); patient/doctor/nurse/bed all required with red `*` and preConfirm validation; doctor + assistant-nurse changed from text input to provider dropdowns via `ipdProviderOptions(_, ['doctor'])` / `['nurse']`. Nurse name saved to new `Admitting_Nurse` column. | `openIpdQuickAdmitModal`, `createIpdQuickAdmission` |
| 2026-06-21 | New i18n keys (en+lo): `ipd.filterActive/Discharged/All`, `ipd.viewHistory`, `ipd.historyChartReadOnly`, `ipd.noDischargedData`, `ipd.assistantNurse`, `ipd.doctorRequired`, `ipd.nurseRequired`, `ipd.bedRequired`, `ipd.searchHint` | `main.js` translations |
| 2026-06-21 | Ward/Bed Management page top-right toolbar: added green "Admit Patient" button → `openIpdQuickAdmitModal()` (was only on Admit Patient page) | `ipd_ward_bed.html` |
| 2026-06-21 | Quick Admit modal: merged "Search registered patient" text input + "Patient" `<select>` into ONE Select2-powered searchable combobox. Uses `dropdownParent: $(Swal.getPopup())` so the popup renders above SweetAlert. Custom `matcher` reads `data-search` blob (Patient_ID + Old_Patient_ID + first/last name) for substring match. | `openIpdQuickAdmitModal` |
| 2026-06-21 | Removed pricing field from Room modal & Rooms table (no billing yet). Deleted: `#ipdRoomCharge` input + `Daily_Charge` from preConfirm payload + `Daily_Charge` cell from `renderIpdRoomsTable`. i18n keys `ipd.chargePerDay`/`ipd.dailyCharge` kept for later. DB column `Daily_Charge` on `Rooms` remains untouched. | `openIpdRoomModal`, `renderIpdRoomsTable` |
| 2026-06-21 | **Removed `ipd_admit` view entirely** (admit now lives on Ward/Bed board's green button). Deleted: `public/partials/views/ipd_admit.html`, navbar item `nav-ipd_admit` + class `mnu-ipd_admit`, `loadIpdAdmitPage`/`renderIpdAdmitPage` functions, view name from both `views` arrays, route handler, visibility refresh block. `createIpdQuickAdmission` now refreshes via `loadIpdWardBedManagement()` instead. IPD nav: 2 items only. | `navbar.html`, `main.js` |
| 2026-06-21 | Ward/Bed board split into TWO segments: General Wards (top, blue gradient header) and VIP Rooms (bottom, gold gradient header with crown icon). VIP detection: `Ward_Type === 'VIP'` OR `Ward_Name` matches /vip/i. New CSS: `.ipd-board-segment`, `.ipd-board-segment-header`, `.ipd-board-segment-vip` (overrides ward header + group bg to gold). New i18n keys: `ipd.generalWardsTitle/Subtitle`, `ipd.vipWardsTitle/Subtitle`, `ipd.wardsCount`. | `renderIpdBedBoard`, `style.css` |
| 2026-06-21 | Added gold **"Add VIP Room"** toolbar button (👑 `fa-crown`) on Ward/Bed page → `openIpdVipRoomModal()`. Extends `openIpdRoomModal(roomId, opts)` with `opts.vipOnly` flag: filters Ward dropdown to VIP wards only, defaults Room_Type='Private', uses VIP title, applies `customClass.popup='ipd-vip-modal'` (gold accent). Editing a VIP room auto-enters VIP mode. Edit/Delete CRUD already present per room via `renderIpdBedBoard`. If no VIP ward exists, blocks with `ipd.noVipWard` warning. New i18n: `ipd.addVipRoom`, `ipd.editVipRoom`, `ipd.noVipWard(Text)`. New CSS: `.btn.ipd-vip-btn`, `.swal2-popup.ipd-vip-modal`. | `openIpdRoomModal`, `openIpdVipRoomModal`, `ipd_ward_bed.html`, `style.css` |
| 2026-06-21 | VIP Room modal: Ward dropdown HIDDEN from user — `Ward_ID` is auto-assigned silently from the first VIP ward (or the room's existing Ward_ID when editing). Switched the visible `<select id="ipdRoomWard">` to a `<input type="hidden">` carrying `autoVipWardId`. Non-VIP modal still shows the dropdown as before. | `openIpdRoomModal` |
| 2026-06-21 | **New view `ipd_config`** — IPD config page under the Settings dropdown (between vaccine_master and locations). Three Bootstrap tabs: Wards / Rooms / Beds, each backed by an existing `renderIpdWardsTable` / `renderIpdRoomsTable` / `renderIpdBedsTable` (previously orphaned). Toolbar reuses Add Ward / Add Room / Add VIP Room / Add Bed buttons. CRUD edit/delete buttons already present in each row's render. New: `loadIpdConfigPage`, nav-item `nav-ipd_config` + class `mnu-ipd_config`, view in both `views` arrays, route handler, i18n keys `ipd.configTitle/Subtitle`, `ipd.wardsTab`, `ipd.roomsTab`, `ipd.bedsTab`. | `ipd_config.html`, `navbar.html`, `main.js` |
| 2026-06-22 | Ward/Bed board page now CLINICAL-ONLY — removed all config CRUD: 4 toolbar add buttons (Add Ward / Add Room / Add VIP Room / Add Bed), ward-header edit/delete (`.ipd-ward-crud`), and room-title edit/delete (`.ipd-room-crud`). Only "Admit Patient" remains in the toolbar. All ward/room/bed CRUD now lives under Settings → IPD Config. Handler functions `deactivateIpdWard/Room` and `deleteIpdWard/Room` still exist (called from the config tables' Edit/Disable buttons). | `ipd_ward_bed.html`, `renderIpdBedBoard` |
| 2026-06-22 | Master Data page (`ຈັດການຂໍ້ມູນ Master`): added "Nurse" (`ລາຍຊື່ພະຍາບານ`) category alongside the existing Doctor category in the `clinical` group. Stored under `Category='Nurse'` in `HIS_One_MasterData`. Also added `<option value="Nurse">` to the hidden `<select id="masterCategory">` in `settings.html` — without it, `$.val('Nurse')` silently no-ops and the category panel stays empty. Generic CRUD (`addMaster`/`editMaster`/`delMaster`) just works. | `masterCategoryGroups` clinical group, `settings.html` |
| 2026-06-22 | **Login auto-sync to Master Data** — when a user with `Role=doctor` or `Role=nurse` logs in (or restores their session), the system automatically inserts their `Name` into `HIS_One_MasterData` with `Category='Doctor'`/`'Nurse'` if not already present. Idempotent (SELECT-then-INSERT, no duplicates). Failures are warn-logged, never block login. Two paths wired: post-password login (`currentUser = buildCurrentUserFromDbRow(user)`) and `restoreLoginSession`. Master Data list thus has TWO sources: manual entries via Settings UI + auto-synced login names. New helper: `window.syncCurrentUserToMasterData(user)`. | `syncCurrentUserToMasterData`, login + session-restore paths |
| 2026-06-24 | **EMR workflow + status pipeline + page rename**. Three coordinated changes: (1) **EMR Discharge Status** dropdown in `emr-modals.html` now has 6 outcomes — ລໍຖ້າຜົນແລັບ, ຮັບຢາກັບບ້ານ, ກວດສຳເລັດ/ກັບບ້ານ, **ສົ່ງເຂົ້າຕິດຕາມ OPD (ໃໝ່)**, **ສົ່ງເຂົ້ານອນ IPD (ໃໝ່)**, ສົ່ງຕໍ່. Emojis dropped from labels (user asked for less "AI-look"). `submitEMRForm` now fires side-effects: `OPD Observation` → `handleEmrSendToObservation()` auto-inserts into `opd_observations` + first doctor note; `Admit IPD` → `handleEmrSendToIpd()` auto-inserts into `HIS_One_Admissions`, copies latest vitals, navigates to Ward/Bed board for bed assignment. `resolveVisitStatusFromDischarge` extended with new mappings `OPD Observation` and `Admit IPD`. (2) **Status pipeline** stays 5-dot visually but the final dot is now context-coloured by `Discharge_Status` via new `resolvePipelineOutcome(visit)` — outcomes: ສຳເລັດ (green check), ຮັບຢາ (green pills), ຕິດຕາມ (amber notes-medical), IPD (red bed), ສົ່ງຕໍ່ (gray ambulance). `renderPipeline(stage, visit)` now accepts the visit row; report renderer updated to pass it. `getPatientStage` extended to map new statuses to stage 5. (3) **"ລາຍງານ" renamed to "ສະຖານະຄິວ"** — page title in `report.html` and navbar label both updated; route `/report` and view ID `report` unchanged (no bookmarks break). | `emr-modals.html`, `submitEMRForm`, `handleEmrSendToObservation`, `handleEmrSendToIpd`, `resolveVisitStatusFromDischarge`, `resolvePipelineOutcome`, `renderPipeline`, `getPatientStage`, `report.html`, `navbar.html` |
| 2026-06-23 | **Reverted navbar to the original flat layout** at user request ("ເອົາ Navbar ກັບມາເປັນແບບເກົ່າ"). The workflow-grouped OPD dropdown (5 items with step badges) was rolled back. Top-level bar now matches the production layout that shipped in `dfc73ac`: Dashboard · ລາຍງານ · ປະຫວັດການກວດ · ຄົນເຈັບ · ຊັກປະຫວັດ · ຫ້ອງກວດ · ວັກຊີນ · ນັດໝາຍ · ຈັດການ IPD ▼ · ຕັ້ງຄ່າ ▼. IPD dropdown label restored to "ຈັດການ IPD" (uses existing `nav.ipdManagement`). All the route plumbing (`HIS_NAV_ROUTES`, `HIS_PATH_ROUTES`, `resolveHisRouteTarget`) and the i18n keys are kept untouched, so `/opd/observation`, `/opd/queue`, `/opd/consultation`, `/ipd/dashboard`, `/ipd/admission`, `/ipd/bed-management`, `/ipd/inpatients`, `/ipd/discharge` all continue to resolve — useful for bookmarks, dashboard cards, and future re-introduction of the workflow grouping. OPD Observation page (`/opd/observation`) is still reachable from the action button on each OPD queue row and from the dashboard's "Observation Patients" KPI card. | `navbar.html` |
| 2026-06-23 | **OPD dropdown now follows clinic workflow order**. User reported "navbar ບໍ່ມີຫຍັງປ່ຽນແປງ" because ຊັກປະຫວັດ and ປະຫວັດການກວດ were still top-level — disconnected from the OPD flow. Moved both into the OPD dropdown and re-ordered to match the real clinic flow: **1️⃣ ລາຍຊື່ຄົນເຈັບ → 2️⃣ ຊັກປະຫວັດ → 3️⃣ ຄິວ OPD → 4️⃣ ຕິດຕາມ OPD** (numbered step badges, items 1-4) followed by a divider and **ປະຫວັດການກວດ** as a historical lookup. Top-level bar is now leaner: Dashboard · ລາຍງານ · OPD ▼ · ນັດໝາຍ · ວັກຊີນ · IPD ▼ · ຕັ້ງຄ່າ. Added `data-i18n` to ນັດໝາຍ and ວັກຊີນ (previously hard-coded). Routes untouched — `/triage` and `/visit_history` still resolve to the same views, just accessed from inside the OPD dropdown. The dropdown's permission gate classes were extended (`mnu-triage mnu-visit_history`) so the OPD dropdown stays visible when a user only has triage/history perms. CSS: new `.his-step-num` 20×20 circular white-on-translucent step badge. | `navbar.html`, `docs/HIS_NAVIGATION_ROUTE_AUDIT.md`, `.his-step-num` in `style.css` |
| 2026-06-23 | **OPD Observation bed board** (Option A — reuse IPD ward/room/bed schema). Per user request, the "ຕິດຕາມ OPD" workflow now has physical beds visualized the same way as IPD. Approach: introduce `Ward_Type='OPD_Observation'` so admins create observation wards/rooms/beds via the existing Settings → IPD Config UI (no new admin screen). New migration `20260623120000_opd_observations_beds.sql` adds `ward_id/room_id/bed_id` columns + index to `opd_observations` and a convenience view `opd_active_observations_by_bed`. New helpers: `window.ipdIsObsWard(ward)` (mirrors `ipdIsVipWard`), `window.obsGetOccupiedBedIds()`, `window.obsActiveObservationByBedId()`. IPD `renderIpdBedBoard` now filters obs wards out (they only render on the OPD page), and `renderIpdSummaryCards` excludes obs beds from the IPD census so KPI numbers stay IPD-only. `openObservationFromVisit` modal now includes a bed picker populated from available obs beds (with "no bed yet (waiting area)" as the default). New `renderObsBedBoard()` draws grid cards per obs ward with status badges, duration counter, and a red ≥6-h highlight that triggers the existing Convert-to-IPD alert. `dischargeObservation` and `convertObservationToIpd` now `NULL` the bed/room/ward to release them automatically. Ward Type dropdown in `openIpdWardModal` extended with `'VIP'` and `'OPD_Observation'`. New i18n keys (en+lo): `obs.assignBed`, `obs.bedOptional`, `obs.noObsWardHint`, `obs.bedBoardTitle`, `obs.bedBoardSubtitle`, `obs.noBedAssigned`. | `ipdIsObsWard`, `obsGetOccupiedBedIds`, `obsActiveObservationByBedId`, `renderObsBedBoard`, `renderIpdBedBoard`, `renderIpdSummaryCards`, `openObservationFromVisit`, `dischargeObservation`, `convertObservationToIpd`, `openIpdWardModal`, `opd_observation.html`, migration `20260623120000` |
| 2026-06-23 | **Menu simplified — Option A (small clinic workflow)**. The OPD/IPD dropdowns had 4+5 items but several pointed to the same underlying view with only a `mode` difference, which confused users. Removed from `navbar.html`: `nav-opd_consultation` (same view as ຄິວ OPD), `nav-ipd_dashboard` (KPI strip is already on the bed board), `nav-ipd_admission` (the green "+ Admit" button on bed board handles it), `nav-ipd_discharge` (the "Discharged" tab inside ລາຍຊື່ IPD handles it). Final menu: **OPD = 3 items** (ຄິວ OPD, ຕິດຕາມ OPD, ລາຍຊື່ຄົນເຈັບ); **IPD = 2 items** (ຈັດການຕຽງ, ລາຍຊື່ IPD). Routes kept in `HIS_NAV_ROUTES`/`HIS_PATH_ROUTES` so `/opd/consultation`, `/ipd/dashboard`, `/ipd/admission`, `/ipd/discharge` still resolve for bookmarked URLs and dashboard cards. i18n keys `nav.opdConsultation`, `nav.ipdDashboardShort`, `nav.ipdAdmission`, `nav.ipdDischarge` also kept for the label-update map and any future re-introduction. Doc updated at `docs/HIS_NAVIGATION_ROUTE_AUDIT.md`. | `navbar.html`, `docs/HIS_NAVIGATION_ROUTE_AUDIT.md` |
| 2026-06-23 | **OPD doctor-room realtime notifications**. When triage submits a visit with `Status='Waiting OPD'` (plus a `Department` value), the doctor at that room now gets an in-page toast + ascending-tone audio cue. Uses Supabase Realtime (`supabaseClient.channel('opd-queue-notifications').on('postgres_changes', ...)` — same pattern as the existing public-queue channel). Subscription is set up once at login (`initApp` calls `setupOpdQueueRealtime()`) and torn down on logout, so notifications fire regardless of the current view. Seed set is filled from a SELECT of all current `Waiting OPD` visit IDs so it doesn't spam toasts for rows that already existed when the doctor logged in. New `#opdMyRoomFilter` `<select>` on the OPD page (populated from `MasterData.Department`) lets each doctor pick "my room"; the value is persisted in `localStorage.his_opd_my_room` so it survives reloads. If "my room" is empty → fire notifications for every room. If set → only fire when `Visits.Department` matches. Toast container is created lazily on `<body>` if not present (works on any page). Tone is two short sine pulses (880 Hz → 1320 Hz) via WebAudio — no audio files needed. | `setupOpdQueueRealtime`, `teardownOpdQueueRealtime`, `showOpdQueueToast`, `playOpdNotificationSound`, `populateOpdMyRoomFilter`, `getOpdMyRoom`/`saveOpdMyRoom`, `loadQueue`, `initApp`, `logout`, `opd.html`, `style.css` |
| 2026-06-23 | **OPD room-specific notification hardening**. Added exact room normalization/matching helpers so alerts only fire when `Visits.Department` equals the selected `localStorage.his_opd_my_room` value; choosing a room now re-seeds existing waiting visits, refreshes the OPD queue, refreshes bell alerts, and requests browser notification permission from the user gesture. Added `handleOpdQueueNotification()` as the single path for realtime and polling, desktop notifications, a 15s polling fallback when Supabase Realtime is unavailable, and teardown cleanup for the poller. `checkAlerts()` now combines appointment alerts with today's `Waiting OPD` rows for the selected room in the top bell dropdown. `loadQueue()` now filters the visible OPD table by the selected room too, so doctors only see and open records for their chosen room unless "all rooms" is selected. Toast CSS was tightened for mobile width and 8px radius. | `checkAlerts`, `normalizeOpdRoomName`, `isOpdRoomMatch`, `seedOpdNotifiedVisits`, `handleOpdQueueNotification`, `pollOpdQueueNotifications`, `setupOpdQueueRealtime`, `teardownOpdQueueRealtime`, `saveOpdMyRoom`, `loadQueue`, `style.css` |
| 2026-06-23 | **Patient registration slow + wrong next ID with 17k+ rows**. (1) `generateNextPatientID` was `.select('Patient_ID')` with no filter/limit — PostgREST capped at 1000 rows and returned the *oldest* 1000 IDs (none with current year prefix), so `maxNum` stayed 0 and every new patient got `LXH2026-000001` instead of continuing the sequence. Fix: filter with `.ilike('Patient_ID', 'LXH<year>%').order('Patient_ID', desc).limit(100)` — O(1) index scan. (2) `initPatientTable` used `fetchSupabaseRows` to paginate the entire table sequentially (17,814 rows = ~18 round-trips × ~500ms ≈ 9 s blocked the UI before DataTables init). Fix: head/count query first to know `numPages = ceil(total/1000)`, then fire ALL page queries in parallel via `Promise.all` + `.range()`. 18 parallel fetches confirmed at ~591 ms by curl (vs ~9 s sequential). DataTables init now also gets `deferRender: true` so only visible rows paint up front. All 17k rows still load — user explicitly asked for "show all but make it faster". | `generateNextPatientID`, `initPatientTable`, `patients.html` |
| 2026-06-23 | **Master-data save was silently failing (Drugs/Labs/Orgs)**. All three tables have NOT-NULL PK columns (`Drug_ID`/`Lab_ID`/`Org_ID`) but the modal save functions never generated an ID — insert returned `code: 23502 null value in column ... violates not-null constraint`. Drugs swallowed the error and showed "ສຳເລັດ" anyway (table stayed empty). Labs surfaced the raw constraint error to the user. Orgs failed silently with no popup. Fix: added generic helper `window.generateNextMasterIDs(table, idCol, prefix, padding, count)` (returns array — also useful for Excel bulk import) and `window.generateNextMasterID(...)` (single). Applied: `submitDrugMasterForm` → `DRUG001`, `submitLabMasterForm` → `LAB001`, `submitOrgForm` → uses user-entered `Org_Code` as `Org_ID` (with `ORG001` fallback). Each form now wraps insert in try/catch and surfaces real error messages instead of fake "ສຳເລັດ". Excel imports for Drugs/Labs/Orgs also generate sequential IDs (one query → array of N IDs). `openOrgModal` now pre-fills the next `ORG001`-style code so users get sequential IDs by default. Extracted `refreshPatientOrgDropdown()` helper and call it after every Org mutation so newly-added orgs appear in the patient registration `<select>` without requiring a relogin. | `submitDrugMasterForm`, `submitLabMasterForm`, `submitOrgForm`, `openOrgModal`, `handleDrugExcelUpload`, `handleLabExcelUpload`, `handleOrgExcelUpload`, `refreshPatientOrgDropdown`, `generateNextMasterID(s)` |
| 2026-06-23 | **Delete buttons on IPD Config tables**. Each row in Wards/Rooms/Beds tabs now has THREE action buttons: Edit (blue pencil), Disable (outline-secondary ban icon → Inactive status), Delete (red trash → hard delete via `deleteIpdWard`/`deleteIpdRoom`/`deleteIpdBed`). Delete functions already existed with guards: ward with rooms blocked, room with beds blocked, occupied/reserved bed blocked. Added `title` tooltips on every action button. New i18n key: `ipd.disable` (en: "Disable", lo: "ປິດໃຊ້ງານ"). Also fixed dangling `common.edit` reference → uses existing `ipd.edit`. | `renderIpdWardsTable`, `renderIpdRoomsTable`, `renderIpdBedsTable` |
| 2026-06-22 | **Add Bed modal — VIP visual cue (no separate button)**. Beds inherit VIP-ness from their parent Ward (`Ward_Type='VIP'` OR Ward_Name matches /vip/i) — no `Bed.Is_VIP` column. Decision: do NOT add a separate "Add VIP Bed" button (would be redundant). Instead: (1) Ward dropdown options for VIP wards get crown emoji prefix + `[VIP]` suffix, (2) `data-vip="1"` on those options, (3) `didOpen` watches Ward change and toggles `.ipd-vip-modal` class on the Swal popup → gold accent kicks in via existing CSS at `.swal2-popup.ipd-vip-modal`. Also extracted the inline `isVipWard` helper that was duplicated in 3 places into `window.ipdIsVipWard` (next to `ipdWardById`/`ipdRoomById`/`ipdBedById`); refactored the bed-board renderer and `openIpdRoomModal` to use it. | `openIpdBedModal`, `openIpdRoomModal`, bed-board renderer, `window.ipdIsVipWard` |
| 2026-06-22 | **IPD Config tab visibility & CRUD reliability fix**. Two issues on the new `ipd_config` view: (1) tabs showed multiple panes simultaneously (Bootstrap/AdminLTE 3 CSS conflict suspected), (2) tab clicks not reliably hiding the previous pane. Fix: defensive CSS `#view-ipd_config .tab-content > .tab-pane:not(.active) { display: none !important; }` + custom JS handler `bindIpdConfigTabs` that bypasses Bootstrap's tab JS — manual class toggle, manual `display` set, plus DataTables `columns.adjust()` after tab show (fixes column-width bugs from tables initialized inside hidden panes). Wired into `loadIpdConfigPage` after the 3 render calls. | `loadIpdConfigPage`, `bindIpdConfigTabs`, `style.css` |
| 2026-06-22 | **Login speed fix** — login was hanging ~15s after correct credentials. Root cause: `seedMasterDefaults` (called inside `initApp` with `await`) loops 11 categories and does a SELECT-then-maybe-INSERT *sequentially*. Even when all categories exist (the steady state on every login), that's 11 sequential Supabase round-trips blocking the UI. Fix: (1) parallelize the existence checks with `Promise.all` + use `select('ID', { head: true, count: 'exact' })` instead of fetching rows; (2) batch INSERTs in one round-trip for missing categories; (3) cache `localStorage.his_master_seeded_v1 = '1'` so subsequent logins skip the check entirely; (4) drop the `await` in `initApp` so seeding runs in background and never blocks login. Net effect: login round-trips reduced from 11+ sequential to 0 (cached after first login). | `seedMasterDefaults`, `initApp` |
| 2026-06-22 | **Cloudflare Pages login-hang diagnosis**. Production (`his.luckxayhospitallaos.com`) was serving the *unbuilt* root `index.html` (which references `/src/main.js`) instead of `dist/index.html` (which references `/assets/index-<hash>.js`). Symptom: clicking "ເຂົ້າສູ່ລະບົບ" did nothing because `window.doLogin` never got defined (the ES-module `/src/main.js` worked as a raw fetch but its `import`/`export` syntax wasn't compiled, so it crashed silently in production). Confirmed via `curl -I /src/main.js` returning 200 from production (should be 404). Fix: in Cloudflare Pages dashboard → Settings → Build configurations, set Build command = `npm run build`, Build output directory = `dist`, Node version 20+. After re-deploy: `/src/main.js` should 404 and `/assets/index-*.js` should serve with `Content-Type: application/javascript`. `dist/` is already in `.gitignore` — Cloudflare must run the build itself. | Cloudflare Pages dashboard (no code change) |
| 2026-06-22 | **Button permissions for IPD modules**. Added 2 new cards to the `buttonPermModal` in `users.html`: `ipd` (view/admit/transfer/discharge/chart_edit) and `ipd_config` (view/add/edit/delete). Wired through `saveButtonPermissions` (new `ipd:` + `ipd_config:` blocks), `resetToRoleDefaults` (all 8 roles get sensible defaults — admin: all-on; doctor: ipd full + ipd_config off; nurse: ipd full minus discharge; lab/pharmacy/staff: all-off; reception: view+admit only; cashier: view only), and `applyButtonPermissions` (7 new CSS hide selectors). Tagged real buttons with classes: `.btn-ipd-admit` on Admit toolbar + Assign/Reserve/Cancel-Reservation dropdown items; `.btn-ipd-transfer`/`.btn-ipd-discharge` on Occupied-bed dropdown; `.btn-ipd-chart-edit` on the 4 chart entry buttons; `.btn-ipd-config-add` on 4 add buttons in Settings → IPD Config; `.btn-ipd-config-edit`/`.btn-ipd-config-delete` on edit/disable buttons in Wards/Rooms/Beds tables + bed action dropdown items. Note: action-menu items pass `className` through `.replace('btn-outline-', 'text-')` then onto the button so multi-class strings like `'btn-outline-success btn-ipd-admit'` survive (only first token rewritten). | `users.html`, `applyButtonPermissions`, `saveButtonPermissions`, `resetToRoleDefaults`, `ipdBedActionItems`, `ipd_ward_bed.html`, `ipd_config.html`, `ipd_chart.html`, `renderIpd{Wards,Rooms,Beds}Table` |

| 2026-06-23 | **Separate OPD Follow-up / Observation module**. Added `opd_observations` and `opd_observation_notes` migration with observation statuses, note types, vital-sign fields, indexes, RLS grants, and duration trigger. Added Patient Management menu with OPD / OPD Follow-up / IPD, new `opd_observation` view, KPI cards, observation list, detail timeline, repeated vitals/notes, discharge, and Convert Observation to IPD. Dashboard/report cards keep Observation counts separate from Active IPD/Admissions/Discharges, and conversion is the only path that inserts into `HIS_One_Admissions`; observation rows never feed bed occupancy, ward occupancy, census, or admission stats. Added Lao/English i18n keys and `opd_observation.*` button permissions. Full implementation and test notes live in `docs/OPD_OBSERVATION_MODULE.md`. | `20260623090000_opd_observations.sql`, `opd_observation.html`, `navbar.html`, `dashboard.html`, `report.html`, `users.html`, `main.js`, `style.css`, `docs/OPD_OBSERVATION_MODULE.md` |
| 2026-06-23 | **HIS navigation workflow regrouping**. Reorganized top navigation into separate OPD (`/opd/queue`, `/opd/consultation`, `/opd/observation`, `/patients`) and IPD (`/ipd/dashboard`, `/ipd/admission`, `/ipd/bed-management`, `/ipd/inpatients`, `/ipd/discharge`) dropdowns. Added route alias/redirect layer so existing view IDs still work while old URLs redirect to canonical workflow URLs. Active-menu highlighting now follows the current route key even when multiple menu items reuse the same view. Dashboard operation cards now navigate to OPD Today, Observation Patients, Active IPD, and Bed Occupancy. Added Lao/English labels and route audit doc. | `navbar.html`, `dashboard.html`, `main.js`, `style.css`, `docs/HIS_NAVIGATION_ROUTE_AUDIT.md` |
| 2026-06-24 | **Queue flow local-date fix for Triage/OPD**. Root cause: visits saved with `toISOString()` around midnight Laos time were stored as the previous UTC date, while Triage/OPD/Dashboard/Report/Public Queue filters queried `YYYY-MM-DDT00:00:00Z` to `T23:59:59Z`, hiding records created between 00:00-06:59 local time. Added local-day helpers (`getLocalDateKey`, `getLocalDayIsoBounds`, `getLocalDateRangeIsoBounds`) and applied them to visit/date queries across Dashboard, alerts, reports, visit history, Triage queue, OPD queue, Observation counts, notification polling, Public Queue, and Activity Log. Triage save now defaults blank target department to `OPD ທົ່ວໄປ` so sent-to-OPD rows are not roomless. Full audit in `docs/HIS_QUEUE_FLOW_AUDIT.md`. | `main.js`, `docs/HIS_QUEUE_FLOW_AUDIT.md` |
| 2026-06-24 | **IPD Ward form simplification**. Removed visible Ward Code (`ລະຫັດຫວອດ`) and Department (`ພະແນກ`) from the IPD Config Add/Edit Ward modal and from the Wards table. `Ward_ID` remains hidden and auto-generated so existing DB relations still work; Department is retained internally for existing rows and defaults to `IPD` on new rows. Validation now asks only for Ward Name. Full audit in `docs/IPD_WARD_FORM_UI_AUDIT.md`. | `main.js`, `ipd_config.html`, `docs/IPD_WARD_FORM_UI_AUDIT.md` |
| 2026-06-24 | **IPD Room form simplification**. Removed visible Room Code (`ລະຫັດຫ້ອງ`) from the IPD Config Add/Edit Room modal and from the Rooms table. `Room_ID` remains hidden and auto-generated so bed relations still work. `renderIpdRoomsTable()` now rewrites the table header before DataTables initializes to avoid stale-column count errors. Validation now asks only for Room Number. Full audit in `docs/IPD_ROOM_FORM_UI_AUDIT.md`. | `main.js`, `ipd_config.html`, `docs/IPD_ROOM_FORM_UI_AUDIT.md` |
| 2026-06-24 | **IPD Bed form code hiding**. Removed visible Bed Code (`ລະຫັດຕຽງ`) from the IPD Config Add/Edit Bed modal and from the Beds table. `Bed_ID` remains hidden and auto-generated so admission, movement, and bed-status relations still work. Bed table rows no longer fall back to showing `Ward_ID` or `Room_ID`; room dropdown labels no longer show `Room_ID` as fallback. `renderIpdBedsTable()` rewrites the header before DataTables initializes to avoid stale-column count errors. Validation now asks only for Bed Number. Full audit in `docs/IPD_BED_FORM_UI_AUDIT.md`. | `main.js`, `ipd_config.html`, `docs/IPD_BED_FORM_UI_AUDIT.md` |
| 2026-06-24 | **IPD Add Bed ward-room sync fix**. The Add/Edit Bed modal no longer keeps a stale room selected after the ward changes. Replaced option hide/show behavior with a full rebuild of `#ipdBedRoom` based on the selected `#ipdBedWard`, so selecting an IPD ward cannot leave ER001 or any room from another ward selected. Added room-required validation for wards with no rooms. Details appended to `docs/IPD_BED_FORM_UI_AUDIT.md`. | `openIpdBedModal`, `docs/IPD_BED_FORM_UI_AUDIT.md` |
| 2026-06-24 | **Navbar OPD Observation dropdown update**. Report label remains `ສະຖານະຄິວຄົນເຈັບ`; the OPD doctor-room link is restored to `ຫ້ອງກວດແພດ (OPD)`; a new OPD Observation dropdown was added as `ຄົນເຈັບນອນຕິດຕາມ OPD` with item `ບອດຕຽງ OPD ຕິດຕາມ` routing to `/opd/observation`; IPD dropdown label changed to `ຄົນເຈັບນອນ IPD`; Vaccines and Appointments remain after IPD. Routes were not changed. Full audit in `docs/HIS_NAVBAR_ORDER_AUDIT.md`. | `navbar.html`, `main.js`, `docs/HIS_NAVBAR_ORDER_AUDIT.md` |

| 2026-06-24 | **OPD Observation bed board now matches IPD format**. The `/opd/observation` page was restyled to use the same visual workflow as IPD bed management: IPD-style workspace header, bed status pills, ward -> room -> bed card grouping, compact bed cards, and action dropdowns. Occupied OPD observation beds show patient/HN, observation number, live duration, and a 6h+ warning when the observation should be considered for IPD conversion. Data separation is unchanged: only `Ward_Type='OPD_Observation'` beds render here, and OPD observation patients still do not count toward IPD bed occupancy, census, admissions, or statistics. MD updated in `docs/OPD_OBSERVATION_MODULE.md`. | `opd_observation.html`, `renderObsBedBoard`, `style.css`, `docs/OPD_OBSERVATION_MODULE.md` |
| 2026-06-24 | **Separated OPD Observation board and list like IPD**. User reported the OPD observation bed board still had a duplicated/list table below it. Removed the table from `/opd/observation` so the bed-board page is board-only. Added a second dropdown item `ລາຍຊື່ຄົນເຈັບນອນ OPD` with route `/opd/observation/list`, new partial `opd_observation_list.html`, and route key `opd_observation_list`. The new list page uses the IPD inpatient-list pattern with date filters, observation table, actions, and detail timeline. Both pages reuse the same `opd_observation` permission and keep Observation patients separated from IPD counts. | `navbar.html`, `opd_observation.html`, `opd_observation_list.html`, `loadObservationPage`, `obsDetailTargets`, `docs/OPD_OBSERVATION_MODULE.md` |
| 2026-06-24 | **Expanded OPD Observation board cards + board action cleanup**. Adjusted the OPD observation board card/grid CSS so OPD beds use the same expanded board dimensions as the IPD bed board: `repeat(auto-fill, minmax(190px, 1fr))`, `8px` gap, and `150px` minimum card height. Removed date-range filters from `/opd/observation` because the board is a live bed-management screen, added the `Observation` button on the board header, and wired it to select an OPD queue patient before opening the existing Observation creation modal. Active bed rows are merged into `window.observationRows` before rendering so board detail/patient names work even when the active observation started before today's list range. | `opd_observation.html`, `openObservationFromBoard`, `openObservationFromVisit`, `renderObsBedBoard`, `style.css`, `docs/OPD_OBSERVATION_MODULE.md` |
| 2026-06-24 | **OPD Observation board switched to IPD detail-card management mode**. User reported the OPD observation cards still looked like the old compact boxes. `renderObsBedBoard()` now renders `ipd-board-mode-detail` and uses the same IPD bed-management card structure: `ipd-bed-top`, `ipd-bed-meta`, `ipd-bed-patient-name`, `ipd-bed-line`, and `ipd-bed-actions`. OPD-specific CSS was extended to mirror the IPD patient-line/action-dropdown/hover/status-badge styling. Available OPD observation beds now expose an IPD-style action dropdown; choosing `Observation` starts the existing OPD observation creation flow and preselects that bed. Occupied beds keep observation-specific actions: open timeline, convert to IPD, and discharge. | `renderObsBedBoard`, `openObservationFromBoard`, `openObservationFromVisit`, `style.css`, `docs/OPD_OBSERVATION_MODULE.md` |
| 2026-06-24 | **OPD Observation actions/tracking aligned with IPD bed management**. User requested all management and follow-up actions to work like IPD. OPD observation bed cards now expose IPD-style actions by status: Available = start Observation + maintenance; Cleaning/Maintenance/Reserved = mark available + maintenance; Occupied = open observation chart/timeline, transfer bed, add vital sign, doctor note, nursing note, medication, procedure, convert to IPD, and discharge/release bed. Added OPD-specific physical bed helpers (`updateObservationPhysicalBedStatus`, `changeObservationBedStatus`) plus `openObservationTransferModal`/`transferObservationBed`, keeping all patient workflow in `opd_observations` and using physical bed status only for room readiness. Discharge and Convert-to-IPD now set the released OPD observation bed to `Cleaning`, matching IPD's release-bed behavior. Board and list detail panels both got the same tracking buttons. | `opd_observation.html`, `opd_observation_list.html`, `renderObsBedBoard`, `openObservationTransferModal`, `transferObservationBed`, `openObservationNoteFromBoard`, `dischargeObservation`, `convertObservationToIpd`, `docs/OPD_OBSERVATION_MODULE.md` |
| 2026-06-24 | **Fixed OPD Observation action dropdown overlap/clipping**. User reported the `ຈັດການ` dropdown was being hidden by the bed-board containers. Root cause was the shared IPD board CSS setting `overflow: hidden` on `.ipd-board-segment` and `.ipd-ward-group`, plus the OPD action menu not having enough stacking priority. Added OPD-only overrides so board segment/body/ward/card/grid allow visible overflow, cards with open dropdowns lift above siblings, and `.ipd-bed-action-menu` uses a higher z-index. | `style.css`, `docs/OPD_OBSERVATION_MODULE.md` |

| 2026-06-24 | **OPD Observation medication action temporarily removed + provider multi-select notes**. User requested medication recording be removed for now, and Doctor/Nursing notes should select doctors/nurses from dropdowns with multiple selection. Removed Medication from OPD Observation bed-card action dropdowns and both observation detail panels. `openObservationNoteModal()` now loads the same provider cache used by IPD, shows a multi-select doctor dropdown for Doctor Note and a multi-select nurse dropdown for Nursing Note, validates that at least one provider is selected, and saves selected provider names to `recorded_by`. Existing medication timeline records remain readable for historical data, but new medication notes cannot be created from OPD Observation UI. MD updated in `docs/OPD_OBSERVATION_MODULE.md`. | `main.js`, `opd_observation.html`, `opd_observation_list.html`, `docs/OPD_OBSERVATION_MODULE.md` |

| 2026-06-24 | **Org Excel import duplicate-key crash fixed**. User uploaded a 29-row Excel where every row shared the same `Org_ID = LXH-AMZ-26-001` (the sheet is "29 customers under 1 organization", not 29 organizations) and that ID already existed in the DB. The old `handleOrgExcelUpload` used `Org_ID` straight from each row and called `.insert()`, so all 29 rows collided on PK and Postgres returned `duplicate key value violates unique constraint "HIS_One_Organizations_pkey"`. Fix: identity-priority chain Cus_ID_Ex > explicit Org_ID > generated `ORG<seq>` > Org_Code (so each customer row gets a unique PK); dedupe in-batch with a `Map`; switch to `.upsert(..., { onConflict: 'Org_ID' })` so re-imports update rather than crash. Saved-message reports how many duplicates were skipped. | `handleOrgExcelUpload` |

| 2026-06-24 | **Epic-style action grouping on OPD Observation bed cards + detail panels**. Per user ask ("ແນະນຳແບບລະບົບສາກົນ"), the action row for an occupied OPD observation bed and the matching toolbar on both detail panels (`opd_observation.html` + `opd_observation_list.html`) now follow Epic/Cerner convention: **(1) Vital Sign** = red standalone button (most frequent, 1-click), **(2) Document** = blue `+ ບັນທຶກ` dropdown with Doctor Note / Nursing Note / Procedure (per-shift documentation), **(3) Bed Management** = dark `ຈັດການຕຽງ` dropdown with Open Chart / Transfer Bed / Convert to IPD / Discharge & Release Bed (workflow that releases the room). New i18n keys `obs.documentNote` (lo: "ບັນທຶກ" / en: "Document") and `obs.bedManagement` (lo: "ຈັດການຕຽງ" / en: "Bed Management"). New CSS `.obs-bed-actions-grouped` flexes the three buttons evenly with ellipsis overflow so they fit on a compact bed card. | `renderObsBedBoard` (occupied branch), `opd_observation.html`, `opd_observation_list.html`, `style.css` (`.obs-bed-actions-grouped`) |

| 2026-06-24 | **View-load cache (60s TTL) — navigating back no longer re-fetches**. User asked "ມີວິທີກັບນ້າແລ້ວບໍ່ໂຫລດຄືນໃໝ່ອີກບໍ່" — going from patient registration back to `/patients` was re-running `initPatientTable()` and fetching 17k rows every time. Added `window.viewLoadCache`, `window.VIEW_CACHE_TTL_MS = 60_000`, `window.VIEW_LIVE` (set of always-reload views), and helpers `shouldLoadView(view, {force})`, `markViewLoaded(view)`, `invalidateViewCache(view?)`. Inside `loadView`, every per-view dispatch is now wrapped in a `_runLoad` gate that skips the fetch when a fresh cache entry exists and stamps the cache after a real load. **Cacheable** (gated): patients, orgs, services, locations, users, vaccine_master, drugs, labs, vaccines, ipd_config, appointments, ipd_inpatient_list, activity_log. **Live** (always reload, bypass cache): dashboard, report, triage, opd, opd_observation, opd_observation_list, ipd_ward_bed, ipd_chart, public-queue, backup. The activity-log Refresh button now calls `invalidateViewCache('activity_log')` before `loadActivityLog()` so user-triggered refresh always hits DB. Mutations should call `window.invalidateViewCache('<view>')` before / after the save to keep nav-back consistent. | `loadView` dispatch block, `activity_log.html` refresh buttons |
| 2026-07-01 | **Triage queue shows new + old patient code**. User asked the Triage (ຊັກປະຫວັດ) list to show BOTH the current code and the legacy code, with the new one bold ("ລະຫັດໃໝ່ຈະເປັນສິເຂັ້ມ blod ໂຕໜາ"). Returning patients carry `Patient_ID` (current, e.g. `LXH2025-001548`) + `Old_Patient_ID` (legacy import code, e.g. `LXH0046792025`) on ONE record — there are NO duplicate old/new record pairs in the DB (verified: 0 of 18k), so nothing to "resolve", just display. `_fetchTriageQueue` now also selects `Old_Patient_ID` and returns `oldId` (normalized) per row; `loadTriageQueue` renders the name cell as: patient name (bold blue) + new/returning badge, then `Patient_ID` in **bold dark** (`fw-bold text-dark`) followed by `(ເກົ່າ: <Old_Patient_ID>)` in muted grey when present. Triage-only (OPD queue unchanged). | `_fetchTriageQueue`, `loadTriageQueue` |
| 2026-07-01 | **QR sticker → code-only big-text card (QR removed)**. Follow-up to the sizing tweak: user asked to drop the QR entirely, make the patient code the first row filling the card width, and enlarge every font ("ຕັດ Barcode ອອກ ເອົາລະຫັດເປັນແຖວທຳອິດຂະຫຍາຍໃຫ້ໃຫຍ່ເຕັມໜ້າ … Font ທຸກໂຕໃຫ້ໃຫຍ່"). (1) `print-areas.html`: each `.patient-card` restructured from left-info + right-QR (row) to a single column — `.pcard-id` (the LXH code) moved to the TOP, `.pcard-right`/`.pcard-qr-wrap` removed, info rows wrapped in `.pcard-info`. (2) `style.css`: `.patient-card` now `flex-direction:column`; `.pcard-id` = 50px/900 weight, centered, bottom-border, fills width; fonts bumped `.pcard-name` 28px, `.pcard-value` 22px, `.pcard-label` 18px; tightened gaps/padding (gap 1mm, card padding 3mm 8mm) so content fits 78mm. Removed all `.pcard-left`/`.pcard-right`/`.pcard-qr-wrap` rules. (3) `main.js` `printQRCard`: removed the `new QRCode(...)` generation loop (element no longer exists) — still sets `#printID{i}` text. Verified in preview: code 50px un-clipped (fills 163mm), card no overflow at 78mm. | `print-areas.html`, `style.css`, `printQRCard` |
| 2026-07-01 | **QR sticker: bigger fonts, smaller QR** (superseded same day by the code-only card above). Bumped `.pcard-*` fonts and shrank QR 45mm→32mm. | `style.css` (`.pcard-*`) |
| 2026-07-01 | **Backup Storage list + download fixed for nested paths**. After wiring Cloudflare Pages env (`BACKUP_GH_TOKEN`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_STORAGE_BUCKET=his-backups`), Backup Now worked (workflow Success) but the **Supabase Storage panel showed "no files"**. Root cause: `backup/scripts/backup_rest.py` uploads to `backups/YYYY/MM/backup-*.zip` (nested), but `functions/api/backup/list.js` listed only the bucket root with `prefix:''` and Supabase Storage `object/list` is NOT recursive, so it found zero zips. Fix: `list.js` now walks the tree (`collectZips`/`listPrefix`, depth cap 5, folders = entries with null `id`) and returns the **full object path** as `name`/`path`. Also fixed `functions/api/backup/signed-url.js`: it did `encodeURIComponent(path)` on the whole path, turning `/` into `%2F` so nested objects couldn't be signed for Download — now encodes each segment and keeps slashes. `restore.js`→`restore_rest.py` already handled nested `BACKUP_NAME` (literal slashes), no change. Backups themselves were always safe in Storage; this was a list/download display bug. | `functions/api/backup/list.js`, `functions/api/backup/signed-url.js` |
| 2026-07-01 | **Patient sticker card: fixed `.pcard-id` code text overlapping its underline in print** ("ສະຕິກເກີ barcode ມັນລົ້ນຈໍ"). User printed a QR/code sticker (Patients → ພິມບັດ) to PDF and the big `LXHyyyy-nnnnnn` code visibly bled into/through the black rule underneath it — text looked doubled/clipped. Root cause: `.pcard-id` was `font-weight: 900` while the page font stack is `'Noto Sans Lao', sans-serif` (`body` rule), and the Noto Sans Lao webfont has no 900 cut — Chrome's print/PDF path synthesized a fake-bold that expanded the glyph bounding box beyond the CSS line-height, so the (tight) line box let the fattened glyphs bleed into the border-bottom rule below. Screen preview looked fine because on-screen rendering didn't show the same synthetic-bold bleed. Fix in `style.css` `.pcard-id`: forced an explicit Latin font (`"Consolas", "Arial", sans-serif` — content is always `LXH` + digits, no Lao glyphs needed), dropped weight to `700` (a real cut, avoids synthesis), font-size 48px→44px, line-height 1.25→1.5, and `padding-bottom` 2mm→`padding: 2mm 0 3mm` for more clearance before the rule. Verified via a standalone preview harness (real `style.css` + sample `.patient-card` markup) — computed style confirmed real 700 weight / Consolas / 66px line-height, no overlap. | `style.css` (`.pcard-id`) |
| 2026-07-01 | **Activity Log "Incorrect column count" fixed + Backup page graceful in local dev**. Two admin-page bugs. (1) **Activity Log**: when a date range had zero logs (default = today, fresh system), `loadActivityLog` rendered a `<tr><td colspan="5">` placeholder and *then* called `.DataTable()` on it — DataTables maps columns from the first body row, sees 1 cell vs 5 headers, and throws the "Incorrect column count" (tn/18) alert. Verified in-page: init on a colspan placeholder throws `_DT_CellIndex`; init on a real 5-cell row is clean. Fix: when `rows.length === 0`, render the empty placeholder and `return` **before** `.DataTable()`. (2) **Backup**: all four panels (status / history / Supabase list / Google Drive list) and the Backup-Now button call Cloudflare Pages Functions at `/api/backup/*`, which don't exist under plain `vite dev` (port 5174). The bare `fetch` threw `TypeError: Failed to fetch` (or got Vite's HTML fallback, breaking `resp.json()`), surfacing as scary red errors. Fix: new `window._backupApiFetch(url, opts)` collapses network-failure / 404 / non-JSON into a single `{ unavailable: true }` sentinel; `window._backupUnavailableRow(colspan)` renders one clean "API ໃຊ້ໄດ້ສະເພາະຕອນ deploy ຫຼື `npm run pages:dev`" row. Rewired `loadLatestBackupStatus`, `renderBackupHistory`, `loadBackupFileList`, `loadGdriveBackupList`, and `runManualBackup` through it. Backup is a deploy-only feature (needs GH token + Supabase service key + Google service account); local dev now degrades gracefully instead of looking broken. | `loadActivityLog`, `_backupApiFetch`, `_backupUnavailableRow`, `loadLatestBackupStatus`, `renderBackupHistory`, `loadBackupFileList`, `loadGdriveBackupList`, `runManualBackup` |
| 2026-07-01 | **OPD Card: patient code moved above barcode + red, Name/Surname merged**. Annotated-screenshot ask: move `ID: LXH2025-001548` up and color it red ("ເອົາ ລະຫັດຄົນເຈັບຂື້ນໄປເທີງ ແລ້ວປ່ຽນເປັນສີແດງ"), drop the separate Surname field into one Name+Surname field ("ນາມສະກຸນຕັດອອກ ປ່ຽນເປັນຊື່ ແລະ ນາມສະກຸນຢູ່ລວມກັນ"). `print-areas.html`: swapped child order in `.opdref-header-right` so `.opdref-cn-row` renders before `svg#popd_patient_barcode` (column flex, so order = visual position); removed the `ນາມສະກຸນ/Surname:` label + `#popd_surname` field, relabeled `ຊື່/Name:` → `ຊື່ ແລະ ນາມສະກຸນ/Name and Surname:`, `#popd_name` now carries new `opdref-fill-fullname` class. `style.css` (both duplicate `#opd-print-area.opdref .X` / `.opdref-page .X` blocks): `.opdref-cn-row`/`.opdref-cn-value` → `color: #dc2626 !important`; added `.opdref-fill-fullname { width: 46mm !important; }`. `main.js`: OPD print binding now sets `popd_name` to `` `${d.First_Name||''} ${d.Last_Name||''}`.trim() ``, dropped the `popd_surname` setText call. Verified via standalone preview harness — computed styles confirmed header-right child order, red `rgb(220,38,38)` on both, 46mm field width, `#popd_surname` absent from DOM. Full notes in `docs/OPD_UI_REDESIGN.md`. | `print-areas.html`, `style.css`, `main.js`, `docs/OPD_UI_REDESIGN.md` |
| 2026-07-01 | **Triage queue: last action button (print OPD/"barcode") floating outside its row**. User circled a detached icon at the far edge of the ຈັດການ column on each Triage row and asked to move it next to the other action buttons, aligned in a row ("ຍ້າຍ Barcode ໄປຢູ່ຕໍ່ຈັດການຂອງໜ້າຊັກປະຫວັດ ແລະ ຈັດແຖວໃຫ້ມັນຊື່ລຽງຕາມກັນ"). Root cause: `loadTriageQueue`'s action cell renders 6 buttons (ເບິ່ງ, ວັດແທກ/edit, ເອີ້ນຄິວ, timeline, delete, "ພິມໃບ OPD" — the OPD-card/barcode print button) inside `<div class="d-flex gap-1 justify-content-center">`, which defaults to `flex-wrap: nowrap`; when the DataTables-sized ຈັດການ column is too narrow for all 6 buttons on one line, the excess (last button) spills outside the cell instead of wrapping, appearing detached from the rest of the row. Reproduced in a standalone harness (real Bootstrap/DataTables/jQuery + the exact button markup) at a constrained column width — with `nowrap` the buttons' bounding rects exceeded the cell's right edge; confirmed fix. Fix in `loadTriageQueue` ([src/main.js](../src/main.js)): action container class → `d-flex flex-wrap gap-1 justify-content-center align-items-center` (wraps buttons onto additional lines within the cell instead of overflowing, and keeps icon-only + icon+text buttons vertically aligned); `#triageTable` DataTable init also gets `autoWidth: false` so the column width tracks actual content instead of a value locked at init time. Verified in the harness: same 6 buttons at the same narrow width now all report `overflowingCount: 0` against the cell bounds. | `loadTriageQueue` |
| 2026-07-01 | **"ພິມ Barcode" (sticker/QR card print) button moved from Patients registration list to Triage queue**. User asked to relocate it ("ພິມ barcode ຍ້າຍຈາກໜ້າລົງທະບຽນໄປຢູ່ຊັກປະຫວັດ"). Removed the `window.can('patients','print_qr')`-gated dark `fa-qrcode` button from the Patients table row actions (`initPatientTable`, [src/main.js](../src/main.js)) and added the same button (same permission check, same `window.printQRCard(patientId)` call, same icon) to the Triage row action group in `loadTriageQueue`, right after the existing "ພິມໃບ OPD" button. Both buttons print different things — "ພິມໃບ OPD" is the full OPD Card (chart form + barcode header), "ພິມ Barcode" is the 3-per-page code sticker (`#print-area`) — Triage now offers both since that's where staff print stickers as patients are checked in. | `initPatientTable`, `loadTriageQueue` |
| 2026-07-01 | **OPD Card page 2: added "ລາຍເຊັນທ່ານໝໍ" (doctor's signature) label**. Annotated-screenshot ask: add a doctor-signature label at the circled empty bottom of the "other" column in the treatment table, just above the Dx/Follow up divider ("ເພີ່ມໃສ່ວ່າ ລາຍເຊັນທ່ານໝໍ"). `print-areas.html`: third `<td>` of `.opdref-treatment-body` (the tall 185mm writing cell) now has class `opdref-doctor-sign-cell` + inner `<div class="opdref-doctor-sign">ລາຍເຊັນທ່ານໝໍ</div>`. `style.css` (both `#opd-print-area.opdref` and `.opdref-page` blocks): `.opdref-doctor-sign` = centered/bold/nowrap/`padding:0 1mm 2mm`; cell bottom-aligned. **Specificity gotcha:** base rule `…opdref-table td` sets `vertical-align: top !important` and its trailing `td` element selector out-specifies a plain `.opdref-doctor-sign-cell` class, so the label rendered at the *top* — fixed by qualifying as `td.opdref-doctor-sign-cell` (matching specificity, later in cascade → wins). Verified in page-2 harness: computed `vertical-align: bottom`, label sits 4px (=2mm padding) above cell bottom, directly over the Dx/Follow up row. Full notes in `docs/OPD_UI_REDESIGN.md`. | `print-areas.html`, `style.css`, `docs/OPD_UI_REDESIGN.md` |
| 2026-07-01 | **OPD Card: show visit date + time under the barcode**. User ask: "OPD Card ເພີ່ມວັນທີ ກັບ ເວລາໃສ່ກ້ອງ barcode". The `#popd_datetime` span already existed but was buried in the page-1 `.opdref-hidden` block (write-only metadata; `main.js:9117` `safeSetText('popd_datetime', "<date> <time>")` — confirmed no code ever reads it back). `print-areas.html`: moved it out of `.opdref-hidden` and re-added as a visible `<div class="opdref-datetime-row" id="popd_datetime">` right after `svg#popd_patient_barcode` inside `.opdref-header-right` — column flex, so it renders directly below the barcode. **No JS change** (same id, same single `safeSetText`). `style.css` (both `#opd-print-area.opdref` and `.opdref-page` blocks): new `.opdref-datetime-row` mirrors `.opdref-cn-row` (Times/Phetsarath serif, centered, `width:62mm` to match barcode, nowrap) but smaller/black — `font-size:11pt`, `font-weight:600`, `color:#000` — reading as a subordinate caption under the red ID + barcode. Full notes in `docs/OPD_UI_REDESIGN.md`. | `print-areas.html`, `style.css`, `docs/OPD_UI_REDESIGN.md` |
| 2026-07-01 | **OPD Card: widened Title field + moved Aged onto the D.O.B row**. Annotated-screenshot ask: "ຂະຫຍາຍຊ່ອງຄຳນຳໜ້າ, ເອົາອາຍຸລົງມາຕໍ່ແຖວວັນເກີດ" — the `ຄຳນຳໜ້າ/Title` fill was too narrow for honorifics like `ທ່ານນາງ`, and `ອາຍຸ/Aged` sat at the far right of the Name row. `print-areas.html`: moved the `ອາຍຸ/Aged:` label + `#popd_age` fill + `ປີ` out of profile row 1 (now just `Title` + `Name and Surname`) into row 2 before `ເພດ/Gender` (row 2 order: `ເກີດ/D.O.B ___ ອາຍຸ/Aged ___ ປີ ເພດ/Gender ☐/☑`). `style.css` (both `#opd-print-area.opdref` and `.opdref-page` blocks): `.opdref-fill-title` width `12mm → 24mm`; no other widths changed (removing Aged from row 1 freed the space). Verified via OPD harness (fill=1): no overflow — row 1 content ends ~162mm, row 2 ~172mm inside sheet, Title fill = 24mm. Full notes in `docs/OPD_UI_REDESIGN.md`. | `print-areas.html`, `style.css`, `docs/OPD_UI_REDESIGN.md` |
| 2026-07-03 | **OPD Card page 2: header fill lines bleed full-width to the paper edge + removed the ວັນເດືອນປີ (date) column**. Annotated-screenshot ask: "ຂະຫຍາຍດ້ານຂ້າງ ຊ້າຍຂວາອອກໃຫ້ສຸດໜ້າ ບ້ານ ເມືອງ ແຂວງ ເບີ ໃຫ້ພໍ ດີ ຕິດຂອບເຈ້ຍ / ລົບຕາຕະລາງວັນເດືອນປີອອກ ຂະຫຍາຍອອກຊ້າຍຂວາໃຫ້ຊີດຂອບເຈ້ຍ ຂະໜາດ A4" — the two patient-header lines (name/DOB/age/sex and village/district/province/phone) left whitespace at the right margin instead of filling the sheet, and the follow-up log's leftmost date column was unwanted. **`print-areas.html`**: `.opdref-followlog-table` dropped its `<col class="opdref-col-fl-date">`, the `<th>ວັນເດືອນປີ</th>` header, and the first `<td>` of `.opdref-followlog-body` — now a 3-column table (ອາການ / ປິ່ນປົວ / ໝາຍເຫດ). **`style.css`** (both `#opd-print-area.opdref` and `.opdref-page` blocks): `.opdref-p2-line` changed from `display:block` (fixed-mm inline-block fills) to `display:flex; align-items:baseline; gap:1.5mm` so the dotted fills flex-grow to the sheet edge; per-field widths replaced with flex — `.opdref-fill-p2-name` `flex:1 1 auto`, `-dob` `flex:0 0 27mm`, `-age` `flex:0 0 9mm`, and the four address fills (`-addr`/`-district`/`-prov`/`-tel`) all `flex:1 1 0` (equal quarters, phone's right edge = sheet edge). Follow-up column widths retargeted from 14/38/36/12% to 43/43/14% (`.opdref-col-fl-symptom`/`-treat`/`-note`; the `-date` rules were deleted). Page-2 sheet already renders at the full 198mm printable width (6mm export margins), so flex fills now bleed edge-to-edge. Verified in a standalone page-2 harness (real `src/style.css`, sample data at 198mm): the four address fills measured equal (145px each), phone right edge == sheet right edge (0px gap), name line grew to fill, and the follow-up table reported exactly 3 headers/3 body cells. Full notes in `docs/OPD_UI_REDESIGN.md`. | `print-areas.html`, `style.css`, `docs/OPD_UI_REDESIGN.md` |

| 2026-06-24 | **Rolled back the Epic-style grouping — back to IPD's single `ຈັດການ ▾` dropdown**. User reviewed both pages side-by-side and asked "ປ່ຽນການຈັດການໃຫ້ເປັນແບບ IPD" — they want the OPD observation bed cards to look exactly like the IPD bed-board cards (one black "ຈັດການ" trigger that opens an 8-item dropdown), not the 3-button Vital/Document/Bed-Mgmt split from the previous entry. The Epic split was theoretically better international UX but did not match the staff's existing habit on this clinic's IPD board. Reverted: occupied-bed action HTML in `renderObsBedBoard` rebuilt to a single status-coloured dropdown with all 8 items (Open Observation, Transfer Bed, Vital Sign, Doctor Note, Nursing Note, Procedure, Convert to IPD, Discharge & Release Bed); both detail-panel toolbars (`opd_observation.html`, `opd_observation_list.html`) restored to the flat outline-button row that matches the IPD chart's toolbar style. Dropped the `.obs-bed-actions-grouped` CSS block. The new i18n keys `obs.documentNote` and `obs.bedManagement` are kept in translations in case the grouped layout is wanted again later. | `renderObsBedBoard`, `opd_observation.html`, `opd_observation_list.html`, `style.css` |

| 2026-06-24 | **OPD Observation modals = IPD parity** ("ທຸກຢ່າງໃຫ້ເປັນແບບດຽວກັນກັບ IPD"). User asked that managing OPD inpatients work the same way as IPD. `openObservationNoteModal()` rebuilt: Doctor Note now uses the IPD SOAP shape (Visit_Type, Diagnosis, Chief_Complaint, S/O/A/P) with multi-select doctor dropdown; Nursing Note uses the IPD shape (Shift, Patient_Condition, Observation, Nursing_Care_Given, Response_To_Treatment, Intake, Output, Pain_Score, Fall_Risk, Allergy_Alert, Medication_Given, Procedure_Done, Notes) with multi-select nurse dropdown; Vital Sign uses the IPD shape (Temp, BP_Systolic+BP_Diastolic split, Pulse, RR, SpO2, Weight, Height, BMI auto-calc, Pain, Consciousness, Notes) with provider multi-select. Modal width bumped to 900 to match IPD. Multi-select preserved (user's previous explicit choice). `renderObservationTimeline()` now renders the structured fields per type instead of a flat `note_text`. New migration `20260624140000_opd_observation_notes_ipd_parity.sql` adds the IPD-parity columns (provider_id/role, visit_type, diagnosis, chief_complaint, subjective/objective/assessment/plan, shift, patient_condition, observation_text, nursing_care_given, response_to_treatment, intake/output, fall_risk, allergy_alert, medication_given, procedure_done, bp_systolic/diastolic, weight, height, bmi, consciousness) to `opd_observation_notes`. Legacy columns (`note_text`, combined `bp`) still populated for backward compatibility. Reused IPD i18n keys (`ipd.visitType`, `ipd.chiefComplaint`, `ipd.subjective`, `ipd.objective`, `ipd.assessment`, `ipd.plan`, `ipd.shift`, `ipd.patientCondition`, `ipd.observation`, `ipd.nursingCareGiven`, `ipd.responseToTreatment`, `ipd.intake`, `ipd.output`, `ipd.fallRisk`, `ipd.allergyAlert`, `ipd.medicationGiven`, `ipd.procedureDone`, `ipd.consciousness`) — no new translations needed. MD updated in `docs/OPD_OBSERVATION_MODULE.md`. | `openObservationNoteModal`, `renderObservationTimeline`, migration `20260624140000_opd_observation_notes_ipd_parity.sql`, `docs/OPD_OBSERVATION_MODULE.md` |

## 12. Conventions When Editing

- **Don't add features beyond ask** — keep diffs tight
- **i18n every UI string** — never hardcode Lao/EN text
- **Use existing helpers**: `ipdEscape`, `ipdFormatDateTime`, `ipdFormDateTimeValue`, `ipdOptions`, `ipdTranslateValue`, `ipdId`, `ipdCalculateBmiValue`
- **CRUD pattern for IPD tables**: `ipdUpsertClinical` / `ipdDeleteClinical` / `ipdSelectClinical` (handles RLS errors + migration check)
- **Sweet Alert for modals** — large forms use `width: 900, html: ..., preConfirm: ...`
- **DataTable** — destroy before init: `if ($.fn.DataTable.isDataTable('#x')) $('#x').DataTable().destroy();`
- **DON'T touch `supabase/restore_chunks/`** — one-time data restore, ignore

## 13. Known Tech Debt / Hidden but Not Deleted

- `loadIpdDashboard()`, `loadIpdDischargePage()` — functions still defined in main.js but no view loads them (orphaned, safe to remove later)
- `renderIpdMedicationOrders`, `renderIpdRadiology`, `renderIpdProcedures`, `renderIpdBilling`, `renderIpdDoctorNotes`, `renderIpdNursingNotes`, `renderIpdVitals`, `renderIpdLabResults`, `renderIpdVisits`, `renderIpdClinicalSummary`, `renderIpdPatientHeader`, `renderIpdClinicalSnapshot` — still exist but `renderIpdChartPage` no longer calls them (only `renderIpdTimeline`)
- `IPD_Visits` / `IPD_Medication_Orders` / `IPD_Radiology_Orders` / `IPD_Procedures` / `IPD_Billing_Items` tables still in DB with data, no UI

## 14. Dev Workflow

```bash
npm run dev              # vite dev server on :5176
npm run build            # → dist/
npm run preview          # preview built dist
```

Supabase: project `pzyrowzghrcfpmhkreag.supabase.co`. Apply migrations via Supabase Dashboard SQL Editor or `supabase db push`.

## 15. Common Tasks Quick Recipes

**Add a field to an IPD entry modal**:
1. Migration: `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ...`
2. Add input to modal `html:` template
3. Add to `preConfirm` payload
4. Update `renderIpdTimeline` body if it should display
5. Add i18n key (en + lo)

**Add a new module page**:
1. `public/partials/views/<name>.html`
2. Add to `views` array in main.js (line ~2206 and ~2861)
3. Add nav item in `navbar.html` with `id="nav-<name>"` and parent `mnu-<name>` class
4. Add route handler `if (v === '<name>') window.load<Name>();` (line ~2906)
5. Write `window.load<Name>()` and CRUD modals
6. Add to permission gates if non-admin should see it

**Patient ID issues** (collision, format):
- Check `generateNextPatientID()` — pattern `^LXH<year>-?<digits>$`, pads to 6
- If existing short IDs exist: run `supabase/fix_patient_id_padding.sql`
- Excel imports use upsert — duplicates within Excel get last-write-wins

## 16. Things NOT to Change Without Asking

- Auth flow (custom users table, not Supabase Auth)
- Patient_ID format (used as FK across 6 tables)
- Table prefix (`HIS_One_`)
- The `dbTable()` wrapper

## 17. Recent Bug Fixes (2026-06-24)

- **Select2 Clear Button**: Fixed an issue where the "Organization Code" dropdown in `patientModal` and `apptModal` was missing the clear (x) button because the default placeholder `<option>` contained text instead of being empty.
- **Organization Dropdown Items Limit/Display**: Removed the `Org_Code` deduplication in the Organization dropdowns. The dropdowns now display individual customers for organizations (using `Org_ID` as the value and showing the contact name alongside the organization code) instead of grouping by `Org_Code`, which previously caused users to mistakenly think the list was limited to 10 items.
- **Organization Fetch Compatibility**: Updated the `fetchOrg` function to query by both `Org_ID` and `Org_Code` (using `.or()`) to maintain backward compatibility with existing patient data that might still store `Org_Code` as their `Organization_ID`.
- **Locations Excel Import ID Generation**: Fixed a "null value in column ID violates not-null constraint" error when uploading Excel files for "Locations" (`HIS_One_Locations`). The system now automatically generates a unique `ID` (e.g. `LOC` + timestamp + index) for each row before inserting it into the database.
- **OPD Toast Recolor (Hospital Logo Palette)**: `.opd-toast` in [src/style.css](src/style.css) restyled from blue gradient to white card with blue left bar (`var(--primary) #1B6BB0`) and red circular icon (`#dc2626`), matching the Luckxay Hospital white/blue/red logo. Department badge kept solid blue with white text; title uses `--primary-dark`; close-button hover turns red.
- **Patient Modal UX Overhaul (2026-06-24 — LOCAL TEST PENDING)**: Five changes to `patientModal` ([public/partials/modals/patient-modal.html](public/partials/modals/patient-modal.html) + [src/main.js](src/main.js)).
  1. **Org clear button moved & enlarged**: Select2 `#p_org_id` now uses `allowClear: false` (the tiny × that dropdown was covering is gone). New big red `<button>` with `<i class="fas fa-trash-alt">` sits as `input-group` prefix in front of `#p_org_name`, calls `window.clearPatientOrg()` → resets `#p_org_id`, `#p_org_name`, `#p_discount_show` and removes their `title` tooltips.
  2. **Discount field scrollable** (revised): the first pass used a readonly `<input>` that only scrolled via arrow keys after focus — too hidden. `#p_discount_show` is now a `<div role="textbox" tabindex="0">` styled by new CSS `.p-discount-scroll` (`overflow-x: auto; white-space: nowrap;` + a thin blue webkit scrollbar). The horizontal scrollbar is visible whenever the discount text overflows, so the user can drag/wheel/touch-scroll left-right to read the whole thing. All JS writers switched from `.val(...)` to `.text(...)` and reset `scrollLeft` to 0 on org change. Hover `title` still mirrors the full text.
  3. **DOB DD/MM/YYYY typeable**: `#dobInput` switched from `type="date"` to `type="text"` with `inputmode="numeric"`, `maxlength="10"`, `placeholder="DD/MM/YYYY"`. New helpers `window.formatDobInput` (auto-inserts the two slashes as you type), `window.dobUiToIso` / `window.dobIsoToUi` (UI ↔ DB conversion). `calculateAgeForm` reads via the converter; `editPatient` fills the input with `dobIsoToUi(d.dob)`; `submitPatientForm` converts back to `YYYY-MM-DD` before save (invalid → `null`).
  4. **Drug allergy → dropdown ມີ/ບໍ່ມີ + conditional detail**: replaced the free-text `p_allergy` with `#p_allergy_choice` select + hidden `#p_allergy` text input. `window.toggleAllergyDetail` shows the textbox only when "ມີ" is chosen. `submitPatientForm` forces the saved value: `ບໍ່ມີ` → literal `'ບໍ່ມີ'`; `ມີ` → user-typed text (or `'ມີ'` if blank). `editPatient` splits the DB value back into choice + detail (anything other than `'ບໍ່ມີ'` or empty → choice=ມີ, detail=value).
  5. **Relationship → master-data dropdown + free typing**: `#p_emer_relation` is now `<select class="dyn-EmergencyRelation">` with a Select2 `tags: true` init (alongside `#p_district`/`#a_patient` etc.), so the user can pick a pre-loaded value OR type a custom one (e.g. ເພື່ອນສະໜິດ, ຍ່າ, ປ້າ) without first adding it in Settings. `editPatient` injects the saved custom value as a fresh `<option>` before calling `.val()` so existing records with non-master values reopen correctly. New master category `EmergencyRelation` wired into `_masterDataFallback`, `seedMasterDefaults` (default values: ພໍ່/ແມ່/ຜົວ/ເມຍ/ລູກ/ພີ່/ນ້ອງ/ຍາດພີ່ນ້ອງ/ໝູ່/ອື່ນໆ), `loadMasterDataGlobalCallback`'s populate-list, `resetMasterDefaults` whitelist, `masterCategoryGroups[patient]` so it appears in **ຕັ້ງຄ່າ → ຄົນເຈັບ → ຄວາມສຳພັນ (ຕິດຕໍ່ສຸກເສີນ)** with full CRUD, AND the hidden `<select id="masterCategory">` in [public/partials/views/settings.html](public/partials/views/settings.html) which I'd missed on the first pass — without that option the Settings sidebar button silently did nothing because `$('#masterCategory').val('EmergencyRelation')` failed on a non-existent option. Seed flag bumped from `his_master_seeded_v1` → `his_master_seeded_v2` so existing installs re-check and insert the new category on next login.
  - **Verified** via Vite preview + fake-DOM harness: DOB round-trip (UI↔ISO + invalid handling), auto-format injection, allergy toggle display/value, clear-org reset, EmergencyRelation visible in `masterCategoryMeta` under the patient group.
  - **Not committed yet** — pending local test by the user before push.
- **OPD Toast Sticky + Per-Visit Dedup**: Removed the 8-second auto-dismiss `setTimeout` in `window.showOpdQueueToast` ([src/main.js](src/main.js)). Toast now stays on screen until the doctor clicks the card or the × button. Added optional 3rd arg `visitId`; toast renders `data-visit-id="<id>"` and the function early-returns if a toast with the same visit id is already in the DOM, so the 60s re-notify cycle doesn't stack duplicate cards (sound + desktop alert still re-fire). `handleOpdQueueNotification` passes `row.Visit_ID` through. Legacy 2-arg calls (test trigger) still work.
- **OPD Notification = One-Time + Sticky Toast (reverted from 60s repeat)**: Earlier in the day this fired every 60s while the patient stayed in `Status='Waiting OPD'`; that was too noisy. Now `handleOpdQueueNotification` in [src/main.js](src/main.js) fires exactly ONCE per visit (existence-gate via `opdNotifiedVisitIds.has(visitId)`), and the sticky toast (still `data-visit-id`-dedup'd, no auto-dismiss) keeps showing until the doctor clicks it. `opdNotifiedVisitIds` reverted from `Map<id,timestamp>` to `Set<id>`; `OPD_NOTIFICATION_REPEAT_MS` and `opdDismissedVisitIds` removed entirely (the dismissed-set was only needed to silence the repeat). `pollOpdQueueNotifications` still prunes the Set when a Visit_ID leaves the queue, so a re-queued visit alerts again as a fresh arrival. `dismissOpdToast(toastId)` simplified — just removes the DOM node, no state-tracking. Verified via fake-clock harness: t=0 fires, t=65s/+10min stay silent, click removes DOM only, post-dismiss stays silent, unrelated visit still alerts.
- **Top Navbar Compact (Logo-only + Shorter Labels)**: Removed `<span id="topnavBrandName">Luckxay Hospital</span>` from [public/partials/navbar.html](public/partials/navbar.html) so only the logo shows (image still has `title="Luckxay Hospital"` tooltip). Added `.his-brand.his-brand-logo-only` CSS variant with tighter padding/no gap. Nav link padding tightened from `6px 12px` to `6px 9px` and gap from 6→5. Shortened labels in both navbar fallback HTML and `window.appTranslations` (lo+en) — `nav.report`: ສະຖານະຄິວຄົນເຈັບ → ຄິວຄົນເຈັບ (en: Queue); `nav.opd`: ຫ້ອງກວດແພດ (OPD) → OPD; `nav.ipdManagement`: ຄົນເຈັບນອນ IPD → IPD; `nav.opdObservationManagement`: ຄົນເຈັບນອນຕິດຕາມ OPD → ນອນ OPD (en: OPD Obs); `nav.patientList` (used by `nav-patients` via `navTextMap`): ລາຍຊື່ຄົນເຈັບ → ຄົນເຈັບ. Result: at 1366×800 the bell + user dropdown now fit fully inside the viewport (verified `getBoundingClientRect().right <= window.innerWidth`).
- **OPD Card Print Rebuild — Match Form 1 reference exactly (2026-06-28)**: Rewrote `#opd-print-area` in [public/partials/print-areas.html](public/partials/print-areas.html) and replaced the entire legacy `#opd-print-area` CSS block at the end of [src/style.css](src/style.css) with a clean `.opdref-*` ruleset. Output now visually matches the user-supplied `Form 1. ໃບ OPD Card.docx.pdf` reference. Key points so future sessions don't re-derive: (1) `@page opd-print { size: A4 portrait; margin: 10mm 12mm }` is the new printable area; (2) every `.opd-page` is hard-clamped to `height: 277mm; overflow: hidden` so 2 OPD pages → exactly 2 PDF pages (the earlier rebuild grew to 4 because content overflowed); (3) dotted form lines are NOT `border-bottom: dotted` (Chrome collapses thin dotted to solid in PDF) — they're a `::after` pseudo-element with an inline SVG circle tiled as `background-image: url(data:image/svg+xml;…<circle r='0.65'/>); background-size: 4px 4px; background-repeat: repeat-x; background-position: left bottom; height: 4px`; (4) Treatment table has ONE `.opdref-treatment-body` row (160mm tall, no internal dividers) + `.opdref-dx-row` (16mm) for `Dx:` / `Follow up:` — matches reference's single big cell; (5) Follow-up table: 4 cols (Time 12% / ອາການ 34% / ຫັດຖະການ 34% / ຜູ້ບັນທຶກ 20%), 8 empty rows at 6.5mm each; (6) Logo (`#print-opd-header-1/-2`), footers, barcode (`#popd_patient_barcode`) and the Patient ID / Date / Vital ID meta line are kept in the DOM (so `safeSetText` and `renderOpdPatientBarcode` in `printOPDCard` keep working) but force-hidden via `display:none !important` because the reference has none of them. All `popd_*` IDs preserved verbatim. Title text changed `ຂໍ້ມູນລົງທະບຽນຄົນເຈັບ` → `ຂໍ້ມູນການລົງທະບຽນ`; corresponding `expectedTitle` and cache-buster (`v=2026-06-28-opd-form1-ref`) in `window.ensureFreshOpdPrintTemplate` in [src/main.js](src/main.js) were updated to match, otherwise the runtime would silently re-fetch the partial each print. Verified with a Chromium harness ([tmp/pdfs/opd-harness.html](tmp/pdfs/opd-harness.html) + `tmp/pdfs/render.cjs`) — both blank and sample-data-filled renders fit 2 pages and bind data correctly.
- **OPD Card → Programmatic PDF (no browser print dialog) (2026-06-28)**: `window.printOPDCard` no longer calls `window.executePrint` (which goes through `window.print()` and lets Chrome stamp date / URL / page numbers in the margins). It now calls new `window.exportOpdCardAsPdf(printPatientId)` in [src/main.js](src/main.js), which uses `html2canvas` + `jsPDF` *directly* — added to [index.html](index.html) alongside the existing `html2pdf.bundle.min.js`. Two passes: each `.opd-page` is force-sized to 186 × 277mm (the A4 printable area at 10/12mm margins), captured by `html2canvas({ width: 703, height: 1047, windowWidth: 703, windowHeight: 1047, scale: 2 })`, then `pdf.addImage(JPEG, 12mm, 10mm, 186mm, 277mm)`. Result is opened in a new tab via blob URL (falls back to download if popup blocked). **Why NOT html2pdf.toCanvas:** the earlier version did `html2pdf().from(clone).toCanvas()`; html2pdf clones the element into its own internal sizing container which clipped each 186mm page to the right half — Test.pdf showed only the right half of each sheet. Calling `html2canvas` directly on the original (not-cloned) element with explicit width/height avoids that whole class of bug. Sheet `padding-top` 9mm (page 1) / 7mm (page 2), `.opdref-treatment-body td` 138mm, `.opdref-dx-row td` 14mm, `.opdref-followup-table tbody td` 6mm (8 rows) — keeps page 2 ≤ 260mm sheet-content, well under the 267mm sheet inner height. Verified with [tmp/pdfs/html2pdf-harness.html](tmp/pdfs/html2pdf-harness.html) → `tmp/pdfs/render-html2pdf.cjs` → `tmp/pdfs/pdf2pages-1x.cjs`: exactly 2 pages, no `localhost` / `Page X of Y` strings in PDF, both pages show full width including left labels, follow-up table bottom border closed.

- **EMR Modal Redesign — "Official HIS" look (2026-06-28)**: Restructured `#emrModal` in [public/partials/modals/emr-modals.html](public/partials/modals/emr-modals.html) into 4 clear sections — (1) dark blue title strip with `ENCOUNTER / ບັນທຶກການກວດ` label + green `OPEN` status pill; (2) patient banner with photo placeholder, big name, HN/demographic chips, and an ALLERGY block (red ribbon if has allergy, kept as-is via `#emrAllergyBox`); (3) vitals strip — CC tile + 8 color-coded vital tiles in a single CSS grid row (icons in different bg colors per vital: red BP, amber Temp, blue PR, cyan RR, green SpO2, purple Wt, orange Ht, gray BMI); (4) clinical body with the existing tabs (`Overview / OPD Card / Lab / Rx`) inside a `.emr-his-body` scrollable wrapper, then a sticky-feeling action footer with a help-info line on the left and ghost `ຍົກເລີກ` + primary `ບັນທຶກຜົນການກວດ` buttons on the right (submit button uses `form="emrForm"` attribute so it can live outside the form tag). All existing IDs (`emrPatientId`, `emrPatientName`, `emrAllergy`, `emrBp`, `emrTemp`, `emrPulse`, `emrResp`, `emrSpo2`, `emrWeight`, `emrHeight`, `emrBmi`, `emrCC`, all overview/lab/rx pane IDs) preserved verbatim so existing JS bindings (`submitEMRForm`, `handleSiteChange`, `openEMRLabModal`, `openEMRDrugModal`, tab counters) keep working. New CSS class prefix is `.emr-his-*` to avoid colliding with existing `.emr-*` rules — added as a new block above the `===== TOP NAVBAR =====` section in [src/style.css](src/style.css). Responsive: below 1100px the vitals strip collapses to 5 columns, below 760px to 4. Verified via standalone harness ([tmp/emr-preview/index.html](tmp/emr-preview/index.html), no Supabase needed) at 1400 / 1280 / 830 px — Overview / OPD Card / Lab / Rx tabs all switch and render the new clinical body correctly.
- **EMR Modal — Tone-down + dedupe (2026-06-28)**: Followup to the redesign — user said the previous version looked too "AI-ish" (gradient title bar, colour-coded per-vital icon backgrounds, glowy status pill, big chips) and that the `OPD Card` tab pane duplicated the data already in the patient banner + vitals strip. Removed the `OPD Card` tab + pane entirely from [public/partials/modals/emr-modals.html](public/partials/modals/emr-modals.html); the `emrOpd*` IDs (`emrOpdPatientId`, `emrOpdPatientName`, `emrOpdGenderAge`, `emrOpdDepartment`, `emrOpdDateTime`, `emrOpdRecordedBy`, `emrOpdBp/Temp/Pr/Rr/Spo2/Weight/Height/Bmi`, `emrOpdCc/Allergy/Disease/Medicine`) are kept in a hidden `.emr-opd-mirror-ids` block so the existing `populateOpdCardTab`-style writers in main.js do not error. Rewrote `.emr-his-*` block in [src/style.css](src/style.css) to a flat clinical look: solid #1f5f97 title strip (no gradient), 4px corner radius, single accent blue for icons, no per-vital colour palette, smaller compact patient banner (44px photo, 3px chip radius), allergy renders as a small red-bordered inline pill, footer/primary button flat with no shadow. Verified in [tmp/emr-preview/index.html](tmp/emr-preview/index.html) at 1280×820: 3 tabs only (Overview / Lab / Rx), vitals strip on one row with 9 cells, banner at 44px height.- **Backup pipeline — two destinations + restore UI (2026-06-29)**: Major overhaul of the backup subsystem. Cloudflare Functions: `functions/api/backup/run.js` and `status.js` had wrong defaults (`it977/HIS-sys` — does not exist); fixed to `ITLXH/HIS-LXH-SYSTEM` and switched the workflow lookup from numeric ID to filename so it survives workflow recreation. New Functions: [functions/api/backup/runs.js](functions/api/backup/runs.js) (GET — last N workflow runs for the History table), [functions/api/backup/list.js](functions/api/backup/list.js) (GET — list ZIPs in Supabase Storage bucket), [functions/api/backup/signed-url.js](functions/api/backup/signed-url.js) (POST — 10-min signed download URL), [functions/api/backup/restore.js](functions/api/backup/restore.js) (POST — triggers restore workflow, requires literal `confirm: "RESTORE"` as safety gate, accepts `{source: "supabase"|"gdrive", backup_name, gdrive_file_id}`), [functions/api/backup/gdrive-list.js](functions/api/backup/gdrive-list.js) (GET — lists Drive folder, uses WebCrypto RS256 JWT signing inside Workers runtime — no Google SDK). GitHub Actions: [.github/workflows/supabase-backup.yml](.github/workflows/supabase-backup.yml) gained "Upload to Google Drive" + "Cleanup old GDrive backups" steps that **skip automatically** when `GOOGLE_SERVICE_ACCOUNT_JSON` secret is missing; summary table now shows both destinations. New [.github/workflows/supabase-restore.yml](.github/workflows/supabase-restore.yml) — workflow_dispatch only (never cron), inputs `source` (dropdown: supabase|gdrive), `backup_name`, `gdrive_file_id`, `dry_run`. New [backup/scripts/restore_rest.py](backup/scripts/restore_rest.py) downloads ZIP from either source (env `BACKUP_SOURCE`), walks `csv/*.csv`, upserts each table via PostgREST with `Prefer: resolution=merge-duplicates` (no TRUNCATE — additive only). UI: [public/partials/views/backup.html](public/partials/views/backup.html) gained a Restore section with 2 side-by-side cards (Supabase Storage green, Google Drive blue `#4285F4`) listing ZIPs with Download + Restore buttons, and the History table now has 6 columns (date, Run #, status, trigger Auto/Manual, duration, link). `renderBackupHistory()` in [src/main.js](src/main.js) switched from localStorage to `/api/backup/runs`. New `loadBackupFileList`, `loadGdriveBackupList`, `confirmRestoreBackup({source, gdrive_file_id})` — the confirm dialog uses SweetAlert input validator forcing the user to type **RESTORE** in caps. New [.dev.vars.example](.dev.vars.example) for local Wrangler dev (`.dev.vars` is git-ignored). Full setup + verification doc at [docs/BACKUP_RESTORE_SETUP.md](docs/BACKUP_RESTORE_SETUP.md).
- **visit_history Respiration column fix (2026-06-29)**: The `loadVisitHistory()`/profile-history SELECT in [src/main.js:4615](src/main.js#L4615) was reading a non-existent column — `HIS_One_Visits.Respiration`. Only `OPD_Vital_Signs` and `TriageLogs` have a column called `Respiration`; the `HIS_One_Visits` table uses **`Respiratory_Rate`**. Changed the SELECT list from `... Pulse, Respiration, Weight ...` → `... Pulse, Respiratory_Rate, Weight ...`. The "ປະຫວັດການມາໂຮງໝໍ" (visit history) tab now loads without the PostgreSQL `column does not exist` error. Note for future writes: when copying SELECT lists between visit/vital-sign features, double-check column naming — the three tables drifted historically.
- **Services Excel import — null ID fix (2026-06-29)**: `handleExcelUpload` in [src/main.js:12019](src/main.js#L12019) was building row objects from the worksheet without generating an `ID` value, so Supabase rejected the insert with `null value in column "ID" of relation "Services" violates not-null constraint`. The single-row `submitServiceForm` at [src/main.js:11225](src/main.js#L11225) already follows the convention `'SRV' + timestamp + '-' + random`; the bulk import path was missing it. Added inside the per-row map: `const idStamp = Date.now().toString(36); row.ID = 'SRV' + idStamp + '-' + i;` (the loop index `i` keeps every row unique within the same batch). Excel "ໂຫຼດຂໍ້ມູນ" on the Services view now succeeds; verified end-to-end with a 5-row sample sheet locally before commit.
- **OPD Card — 8-point fix round (2026-06-30)**: Round of fixes from the user's annotated PDF preview. (1) **Surname** — `popd_surname` was inside the hidden `.opdref-hidden` block (template carried over from an old layout); moved out and added a visible cell on the Name row so `d.Last_Name` renders. (2) **Gender ticks** — the gender cell was a static `□ ຊາຍ/Male  □ ຍິງ/Female` with no JS hooks; replaced with two ID'd span checkboxes `popd_gender_male` / `popd_gender_female`, and added a check-state binder in `printOPDCard` that matches `M/Male/ຊາຍ` and `F/Female/ຍິງ` case-insensitively, writing `☑` to the matched one and `□` to the other. (3) **Address split** — `popd_district` and `popd_prov` were also hidden; moved out and added a single row "ບ້ານ/Village ___ ເມືອງ/District ___ ແຂວງ/Province ___" with three independent fills bound to `d.Address`, `d.District`, `d.Province`. (4) **Emergency contact row** — none existed; added one row "ຜູ້ຕິດຕໍ່ສຸກເສີນ/Emergency ___ ເບີໂທ/Phone ___ ສາຍສຳພັນ/Rel ___" bound to `d.Emergency_Name`, `d.Emergency_Contact`, `d.Emergency_Relation` (the Phones/Patient columns confirmed via `Emergency_Name/Contact/Relation` in [src/main.js:5911](src/main.js#L5911)). (5) **Discount** — was reading `d.Discount`, but `Discount` lives on the `Organizations` table, not `Patients`, so it always rendered blank for org-affiliated patients. Added an async lookup in the OPD print path: when `d.Organization_ID` is set, query `Organizations` for `Discount` via `Org_ID.eq` or `Org_Code.eq`; fall back to `d.Discount` if present, else `-`. (6) **BMI column** — `popd_bmi` was hidden; promoted to the 8th column of the vital signs table; BMI text trimmed from `"22.0 (ປົກກະຕິ)"` to `"22.0"` to fit the narrower cell. (7) **Vital table sizing** — table cell font dropped from 11.5pt to 10pt and side padding from 0.5mm to 0.3mm to fit 8 columns on the 186mm inner page width. (8) **BP 120/60 false-low warning** — [src/main.js:6106](src/main.js#L6106) `s <= 90 || d <= 60` was firing for any BP whose diastolic was exactly 60 (a normal value); changed to strict `<` so 90/60 boundary readings no longer trigger the "ຄວາມດັນຕ່ຳ" SweetAlert. Template cache-buster bumped to `2026-06-30-opd-emer-bmi-gender-v1`. CSS field widths: `fill-village 34mm / district 32mm / prov 40mm` (district names short, province names long); `fill-emer-name 38mm / phone 30mm / rel 22mm`. Verified end-to-end in [tmp/pdfs/opd-harness.html](tmp/pdfs/opd-harness.html) (Puppeteer headless Chrome → A4 PDF, displayHeaderFooter:false, margin 10/12mm) — all 8 issues resolved, both Village/District/Province and Emergency rows fit one line each.
- **OPD Card — Title prefix + Org ID width (2026-06-30)**: Two more user comments on the rendered OPD card. (1) **Title prefix** — there was no `ຄຳນຳໜ້າ/Title` field before the Name field, so honorifics like ທ່ານ / ນາງ / ດຣ. did not appear. Added `<span id="popd_title" class="opdref-fill opdref-fill-title"></span>` at the start of the Name row in [public/partials/print-areas.html](public/partials/print-areas.html), bound via `safeSetText('popd_title', d.Title || '')` in [src/main.js](src/main.js) (`printOPDCard`), and added `.opdref-fill-title { width: 12mm }` in [src/style.css](src/style.css) (both `#opd-print-area.opdref` and the `.opdref-page` fallback). To fit the new column on the same single-line row inside the 186mm inner page width, also tightened `opdref-fill-name 36→24mm` and `opdref-fill-age 16→10mm`. Verified row geometry via [tmp/pdfs/check-row.cjs](tmp/pdfs/check-row.cjs): row ends at 661/665 px (4 px slack). (2) **Org ID truncation** — `popd_org_id` was rendering as `CUS-LXH-AM` for the org `CUS-LXH-AMZ-12345` because `.opdref-org-row #popd_org_id` flex-basis was 28mm. Bumped to **44mm** in both `#opd-print-area.opdref` and `.opdref-page` overrides; flex container balances the now-shorter Org Name fill via its `1 1 auto` flex setting. Verified the long sample ID renders without truncation. Template cache-buster bumped to `2026-06-30-opd-title-orgid-v2`.
- **OPD Card — CC multi-line + Nutrition checkboxes + Page 2 5-col follow-up + QR sticker enlarged (2026-06-30)**: Three more user comments rolled into one round. (1) **Chief complaint multi-line** — `popd_cc` used to be one short underline that truncated when the symptoms ran long; split into two stacked full-line rows (`.opdref-fill-cc` 124mm on the first row, blank `.opdref-fill-cc-line` 182mm on the next) so the nurse can write 2 lines on the printed paper. (2) **Nutrition status redesigned** — old single underline row `Nutrition status: ____` replaced with a Lao label + 3 checkbox options + free-text "ອື່ນໆ": `ໂພຊະນາການ/Nutrition status: ☐ ປົກກະຕີ/Normal  ☐ ບໍ່ປົກກະຕີ/Abnormal  ☐ ອື່ນໆ/Other: _____`. New IDs `popd_nutri_normal`, `popd_nutri_abnormal`, `popd_nutri_other`, `popd_nutri_other_text` — checkboxes stay blank (□) by default since there is no DB column for this (manual tick). (3) **Page 2 — wider treatment table + 5-column follow-up table** — `.opdref-treatment-body td` height bumped 168mm → 186mm to give the doctor more writing room. Follow-up `<colgroup>` extended from 4 cols to 5: `Time | ອາການ | ຫັດຖະການ | ໝາຍເຫດ | ຜູ້ບັນທຶກ` (new `.opdref-col-note { width: 22% }` column). Existing column widths rebalanced: time 11.5→10%, symptom 46.5→32%, procedure 28→22%, note 22%, recorder 14%. Body row count reduced from 8 → 5 rows so it still fits on page 2 below the now-taller treatment table. (4) **Patient QR sticker enlarged** — old print-area at [public/partials/print-areas.html:2](public/partials/print-areas.html#L2) printed three 65×35mm cards which barely filled the top third of A4 (the rest blank). Card size enlarged to **180×85mm** (3 cards stack vertically = ~270mm, fills almost the whole A4 portrait height with 6mm gaps + 6mm padding). QR code increased from 30×30 px to 200 px (rendered at 50×50mm), font sizes bumped (label 7→11px, value 9.5→14px, name 10.5→18px, ID 10→14px). `new QRCode(... { width: 30 })` at [src/main.js:5982](src/main.js#L5982) → `width: 200, height: 200`. Template cache-buster bumped to `2026-06-30-opd-cc-nutrition-page2-v3`. Verified end-to-end via two harnesses: [tmp/pdfs/opd-harness.html](tmp/pdfs/opd-harness.html) (OPD card PDF via Puppeteer print) and [tmp/pdfs/qr-harness.html](tmp/pdfs/qr-harness.html) (QR card screen screenshot — print emulation hides the area, so we use a screen-mode renderer in [tmp/pdfs/render-screen.cjs](tmp/pdfs/render-screen.cjs)). All 4 items render as expected.
- **OPD Card — Page 2 title rename + push follow-up to page bottom (2026-06-30, NOT committed yet)**: User asked to rename "ໃບຕິດຕາມອາການ ແລະ ຫັດຖະການ" → **"ໃບບັນທຶກພະຍາບານ"** (Nurse's record), pin that table to the bottom edge of A4 page 2, and let the upper Treatment table grow into the freed space. Title text changed in [public/partials/print-areas.html](public/partials/print-areas.html). Layout in [src/style.css](src/style.css): `.opdref-sheet-page2` now uses `display: flex; flex-direction: column; min-height: 277mm` (A4 portrait inner height) so the page 2 sheet behaves as a flex column. `.opdref-page2-title-gap` (= the bottom-section title) now has `margin-top: auto` which absorbs all remaining vertical space and pins the entire nurse's-record block (title + 5-column table) to the bottom edge. Treatment body height set to **185mm** (started at 193mm but the user reported the bottom border of the nurse table was getting clipped — "ເສັ້ນລຸ່ມມັນຫາຍໄປໜ້ອຍໜື່ງ" — total layout overflowed the 277mm budget; tuned down to 185mm restores a small bottom safety margin while still giving the doctor more writing room than the previous 168mm). Both CSS blocks updated (`#opd-print-area.opdref` and the `.opdref-page` fallback). Verified in [tmp/pdfs/opd-nurse-record-page2.png](tmp/pdfs/opd-nurse-record-page2.png): bottom section pinned at the page edge, Treatment area fills the top, 5-column follow-up rows all visible. **NOT pushed to git** — per user "ບໍ່ຟ້າວ commit". Will be folded into the next commit.
- **QR sticker fix — overflow to 2 pages + OPD card bleed (2026-06-30, NOT committed yet)**: User-supplied PDF "Stickler.pdf" showed the QR-sticker print was 4 pages instead of 1 — page 1 had 2 stickers, page 2 had the 3rd, pages 3–4 were the OPD card bleeding into the print. **Root cause #1 (overflow)**: a `@page { margin: 10mm 12mm }` block at [src/style.css:3679](src/style.css#L3679) (added during the OPD card redesign) was overriding the original `@page { margin: 4mm }` default from line 2603. OPD doesn't actually need it any more — OPD is rendered via Puppeteer's own `pdf({ margin })` parameter, not CSS `@page`. The leftover 10mm/12mm shrank the QR sticker's printable area from 289mm × 202mm to 277mm × 186mm; 3 × 85mm cards + gaps + padding = 279mm overflowed 277mm by 2mm and pushed the third card to page 2. Removed that default `@page` block (the 4mm default from line 2603 now applies again). **Root cause #2 (OPD bleed)**: not fully reproducible locally — executePrint correctly sets inline `display: none` on `#opd-print-area` and the `@media print { .print-container:not(.print-active) { display: none !important } }` rule reinforces it. Added a redundant belt-and-suspenders rule inside the same `@media print` block: `#opd-print-area:not(.print-active), #vac-print-area:not(.print-active), #print-area:not(.print-active) { display: none !important; visibility: hidden !important; }`. **Card geometry tightened** to comfortably fit 3 cards on one page with a safety margin: card height 85mm → **78mm**, padding 6mm/8mm → 5mm/7mm, grid gap 6mm → 4mm, grid padding 6mm 0 → 2mm 0, QR side width 56mm → 50mm, QR canvas 50mm → 45mm. New total: 3 × 78 + 2 × 4 + 4 = 246mm — fits 277mm budget with 31mm slack. Verified in [tmp/pdfs/qr-harness.html](tmp/pdfs/qr-harness.html) updated to mimic `executePrint()` (set `.print-active` on `#print-area` + remove others) before Puppeteer enters print emulation; output [tmp/pdfs/opd-qr-tight.pdf](tmp/pdfs/opd-qr-tight.pdf) is **1 page** with all 3 stickers, no OPD bleed. **NOT pushed to git** per the user's earlier "ບໍ່ຟ້າວ commit".
- **Dashboard — remove duplicate top KPI row + flatten "AI-look" (2026-06-30)**: User's annotated screenshot crossed out the top operational KPI strip (OPD Today / Observation / Active IPD / Bed Occupancy) as a visual duplicate of the in-report KPI tiles (Total Visits / New / Returning / Ins-Corp), and asked for a more formal, less "AI" dashboard. (1) Removed the `obs-kpi-grid dashboard-ops-grid` block from [public/partials/views/dashboard.html](public/partials/views/dashboard.html). (2) In [src/main.js](src/main.js) `fetchDashboardData`: dropped the `#dashOpdToday…` spinner-init line and the now-orphaned `window.updateDashboardOperationalStats(...)` call so the dashboard no longer fires the extra Supabase beds/admissions/observation queries (function definition left in place, just uncalled). (3) Flattened the on-screen ("not export mode") dashboard CSS in [src/style.css](src/style.css) per [feedback_no_ai_looking_ui]: KPI tile background `radial-gradient + linear-gradient` → solid `#ffffff`, radius 6→4px; report panel / split-card `box-shadow: 0 10px 22px…` → `none`, radius 6→4px, border `rgba(27,107,176,.14)` → `#e2e8f0`. Nav to OPD queue / Observation / IPD / beds still reachable from the sidebar. `node --check src/main.js` passes. Full detail in [docs/OPD_UI_REDESIGN.md](docs/OPD_UI_REDESIGN.md).
- **OPD Card — Page 2 rebuilt as "ໃບຕິດຕາມອາການປິ່ນປົວ" follow-up log (2026-07-03, NOT committed yet)**: Head-of-department comment: replace the whole Page 2 with the paper reference form. Old Page 2 (`ໃບບັນທຶກອາການ ແລະ ການປິ່ນປົວ` 3-col History/Treatment/other + Dx/Follow-up row + separate `ໃບບັນທຶກພະຍາບານ` 5-col table) removed from [public/partials/print-areas.html](public/partials/print-areas.html). New Page 2 = title **ໃບຕິດຕາມອາການປິ່ນປົວ**, a 3-line patient header (`ຊື່ ນາມສະກຸນ/Name` + `ວ/ດ/ປ/D.O.B` + `ອາຍຸ/Age … ປີ`; `ທີ່ຢູ່/Address` + `ເມືອງ/Dist` + `ແຂວງ/Prov` + `ໂທ/Tel`; `Diagnosis/ມະຕິແພດ` full-line), then ONE open 4-column log table (`ວັນເດືອນປີ | ອາການ | ປິ່ນປົວ | ໝາຍເຫດ`, single tall body cell), then a signature row (`ລາຍເຊັນພະຍາບານຮັບຜິດຊອບ` left / `ລາຍເຊັນທ່ານໝໍ` right). New IDs `popd2_name/dob/age/village/district/prov/phone/dx` are bound in `printOPDCard` ([src/main.js](src/main.js), right after `popd_doctor`) so the sheet is usable stand-alone. CSS: replaced the old page-2 rules (`opdref-col-history/treatment/other`, `opdref-treatment-body`, `opdref-doctor-sign`, `opdref-dx-row`, `opdref-col-time/…/recorder`, `opdref-followup-table`) with `opdref-p2-header/p2-line/fill-p2-*`, `opdref-col-fl-*` (14/38/36/12%), `opdref-followlog-body td { height: 214mm }`, and `opdref-p2-sign-row` — in BOTH the `#opd-print-area.opdref` block and the `.opdref-page` fallback ([src/style.css](src/style.css)). **Fit tuning**: the four bilingual header labels initially overflowed the 180mm row width (row1 by 30mm, row2 by 29mm — measured via new [tmp/pdfs/measure-p2.cjs](tmp/pdfs/measure-p2.cjs)); fixed by abbreviating labels (`Name Surname→Name`, `ວັນ/ເດືອນ/ປີ→ວ/ດ/ປ`, `District→Dist`, `Province→Prov`, `ເບີໂທ→ໂທ`) and sizing fills to name46/dob28/age10 (row1) and addr26/dist22/prov26/tel28 (row2) → all rows now 0 overflow. Cache-buster bumped `2026-06-30-opd-cc-nutrition-page2-v3` → **`2026-07-03-opd-page2-followup-log-v4`** in `data-opd-template-version`, `PARTIAL_CACHE_BUST`, and `expectedVersion`. Verified end-to-end via the Puppeteer harness ([tmp/pdfs/opd-harness.html](tmp/pdfs/opd-harness.html) `?fill=1` → render.cjs → pdf2pages.cjs → [tmp/pdfs/p2v5-page2.png](tmp/pdfs/p2v5-page2.png)): output is exactly **2 pages**, all header fields render without clipping, log table fills the page, signatures pinned near the bottom. **Follow-up (same day)**: head marked the live export "move up + expand L/R" — `.opdref-sheet-page2` widened `180mm → 186mm` (fills the full printable area, was centered with a 3mm side gap), top padding `4mm → 0.5mm`, and the freed 6mm reallocated to fills (name 50 / dob 30 / prov 29 / tel 31mm). Any wider/higher than this needs reducing the `exportOpdCardAsPdf` 12mm/10mm print margins. **Follow-up 2 (same day)**: user chose to expand Page 2 further without touching Page 1, so `exportOpdCardAsPdf` now uses **per-page margins** — `PAGE_MARGINS = [{x:12,y:10},{x:6,y:6}]` via a `pageGeom(i)` helper feeding both the html2canvas capture size and the `pdf.addImage` placement. Page 2 is captured at 198×285mm and placed at (6,6); Page 1 stays 186×277 at (12,10). Because `.opdref-page`/`.opdref-sheet-page2` widths are `!important`, the export now forces the page + sheet width with `setProperty(...,'important')` (was `Object.assign`, which the !important CSS silently overrode) and restores the sheet cssText in `finally`. Verified by running the real export path headlessly ([tmp/pdfs/export-verify.cjs](tmp/pdfs/export-verify.cjs) — injects real html2canvas 1.4.1 + jsPDF 2.5.1 from CDN) → 2 pages, Page 2 reaches ~6mm from every edge, Page 1 unchanged. **Follow-up 3 (same day)**: head reworked the Page-2 header — Title merged into the Name fill (`popd2_name` = Title+First+Last), Gender ticks added to row 1 (`popd2_gender_male/female`), row 2 = Address/District/Province only, and Tel moved onto the Diagnosis row (`#popd2_phone` pinned `flex:0 0 34mm` in the full-line row so ມະຕິແພດ fills the rest). Name label shortened `ຊື່ ນາມສະກຸນ/Name` → `ຊື່/Name` to fit Gender; fills re-tuned; cache-buster → `2026-07-03-opd-page2-followup-log-v5`. Re-verified via export-verify.cjs ([exp2-page2.png](tmp/pdfs/exp2-page2.png)): 3 header rows 0 overflow, 2 pages, Page 1 intact. **Follow-up 4 (same day)**: name kept clipping (labels ate ~78mm, name fill only 42mm), so row-1 labels shortened to Lao-primary (`ຊື່/Name:`→`ຊື່:`, `ວ/ດ/ປ/D.O.B:`→`ວ.ດ.ປ:`, `ອາຍຸ/Age:`→`ອາຍຸ:`, `ເພດ/Gender:`→`ເພດ:`) and name fill widened `42mm → 78mm` — the short `ຊື່:` label also shifts the name field ~12mm left as requested. Rows 2/3 stay bilingual. Verified [exp3-page2.png](tmp/pdfs/exp3-page2.png): full name on a long line, 0 overflow, 2 pages, Page 1 intact. **Follow-up 5 (same day)**: head replaced the Diagnosis field with the full vital-sign set — row 3 is now `ໂທ:` + phone + `ອາການຊີວິດ/Vital:` inline chips for all 8 vitals (`popd2_temp/bp/pr/rr/spo2/w/h/bmi`, bare values, BMI reuses page-1 `bmiText`); `popd2_dx` removed. New `.opdref-p2-vital`/`.opdref-p2-vital-line` CSS (11pt, 1.5mm chip gap), Tel fill trimmed to 28mm so all 8 vitals + Tel fit one line. Cache-buster → `2026-07-03-opd-page2-followup-log-v6`. Verified [exp5-page2.png](tmp/pdfs/exp5-page2.png): 0 overflow, 2 pages, Page 1 intact. **Follow-up 6 (same day)**: head asked to put ບ້ານ/ເມືອງ/ແຂວງ/ໂທ all on one row and render vitals as the **Page-1 8-column table** (not inline chips). Header row 2 now = `ບ້ານ·ເມືອງ·ແຂວງ·ໂທ`; the inline vital chips replaced by a copy of Page-1's `.opdref-vital-table` (+`ອາການຊີວິດ/Vital sign:` label), bindings updated to Page-1 format (units in cells). Log body `214mm → 196mm` to fit the vital table at 2 pages; dead `.opdref-p2-vital*` CSS removed; cache-buster → `2026-07-03-opd-page2-followup-log-v7`. Verified [exp6-page2.png](tmp/pdfs/exp6-page2.png): address+Tel one row, 8-col vital table identical to Page 1, 2 pages, Page 1 intact. **Follow-up 7 (same day)**: head asked to pull the vital table up (tighten header gaps) to give the log table more room — reduced Page-2 spacing (title mb 4→2mm, `.opdref-p2-header` 3→1mm, `.opdref-p2-line` 2.5→1.2mm, vital-title 1.5→1mm) and raised the log body `196mm → 210mm`. Verified [exp7-page2.png](tmp/pdfs/exp7-page2.png): compact header, taller log table, 2 pages, Page 1 intact. **Follow-up 8 (same day)**: head asked to add the logo + code(ID+barcode) + date to Page 2. Replaced the plain title with a Page-1-style `opdref-header-row` — logo (`#print-opd-p2-logo`), centered title, red `#popd2_cn` + `#popd2_barcode` + `#popd2_datetime`. `renderOpdPatientBarcode(patientId, svgId)` generalised (2nd arg defaults to `popd_patient_barcode`, so Page 1 unchanged); new bindings in `printOPDCard` set logo (`page1HeaderSrc`), ID, date, barcode. Compact `.opdref-p2-header-row` CSS (logo 36×16mm, barcode 54×10mm, ID 12pt, date 9pt, title 16pt); log body `210mm → 194mm` so sheet content = 277.3mm (fits the 285mm Page-2 export area). Cache-buster → `2026-07-03-opd-page2-followup-log-v8`. Verified [exp9-page2.png](tmp/pdfs/exp9-page2.png) (barcodes via real JsBarcode 3.11.6): full header, signatures visible, 2 pages, Page 1 intact. **Follow-up 9 (same day)**: head asked to raise/enlarge the logo — `.opdref-p2-header-row` set `align-items:start`, logo cell `align-items:flex-start` (top-aligned with the ID), title `align-self:center`, logo `36×16mm → 42×20mm`, first col `40→44mm` (CSS-only, no version bump). Verified with the real [luckxay-logo.jpg](public/luckxay-logo.jpg) ([expB-page2.png](tmp/pdfs/expB-page2.png)): logo level with ID, clean, 2 pages, Page 1 intact. **Follow-up 10 (same day)**: "logo still not nice" → enlarge to Page-1 size. The logo is near-square (444×416, aspect 1.07); the `max-height:20mm` cap had squashed it. Raised logo `max-height 20→32mm`, width `42→44mm`, first grid col `44→46mm`; log body `194→184mm` to compensate (sheet content 278.6mm, within the 285mm Page-2 area). Verified [expC-page2.png](tmp/pdfs/expC-page2.png): full logo at Page-1 size, top-aligned with ID, 2 pages, Page 1 intact. **NOT pushed to git** — per user "ກວດສອບໃນ local ກ່ອນບໍ່ຟ້າວ commit".
- **Dashboard — INS/CORP KPI stuck at 0 (2026-07-02, NOT committed yet)**: User reported the `INS / CORP` tile (`#dash-inscorp`, "ລູກຄ້າອົງກອນປະກັນໄພ") always showed 0. Root cause in `renderDashboardCharts` ([src/main.js:4238](src/main.js)): the count filtered on **visit-level** fields `v.Revenue_Group` / `v.Visit_Type`, but `HIS_One_Visits` carries neither column, so the filter never matched. Insurance/corporate status is a property of the **patient** (`Insurance_Company`, `Organization_ID`, `Name_Org`), which the dashboard already attaches to each visit as `v.Patients` (set at [src/main.js:4054](src/main.js)). Fix: added an `insCorpHas(val)` helper + `insCorpNone` set (treats `''`, `-`, `ບໍ່ມີ`, `none`, `n/a`, `self pay`/`self-pay`, `ຈ່າຍເອງ` as non-insurance, case-insensitive) and rewrote the filter to count a visit when `v.Patients.Insurance_Company` OR `Organization_ID` OR `Name_Org` is a real value; the old Revenue_Group/Visit_Type check is kept only as a legacy fallback. Verified: dev server (port 5176) compiles with no console/build error, and a standalone node unit test of the filter counted 4/4 correctly (insurance, corporate, org-only, legacy package counted; self-pay/none/empty/dash excluded). **NOT pushed to git** — per user "ກວດສອບໃນ local ກ່ອນບໍ່ຟ້າວ commit"; pending the user's test against real data.
- **Dashboard PDF mirrors on-screen layout (2026-06-30)**: User's rendered PDF showed the export did not match the live dashboard — the 12-column grid (Top 8 + Time Slot side-by-side) collapsed to a single stacked column and the KPI tiles rendered FontAwesome spinners instead of numbers. Root cause in `window.exportDashboardPDF` ([src/main.js](src/main.js)): it added `dashboard-export-mode` (the on-screen 12-col grid is defined only on `#dashboardPrintArea…:not(.dashboard-export-mode)` in [src/style.css](src/style.css) ~6615, so the class removed it) AND captured at `windowWidth:1122px`, which is below the `@media (max-width:1199px)` breakpoint (~6696/6736) that collapses the grid. Fix: stop toggling `dashboard-export-mode` (capture the page as-shown), capture with `windowWidth:1485` (> the 1199 breakpoint) so the desktop 12-col grid is preserved while still cropping the canvas to 297×210mm (1122×794px) for true A4-landscape proportions. Also added a pre-capture guard that waits ≤4s for `.fa-spinner` to clear from `#dashboardPrintArea` so KPI numbers (not spinners from the 120s auto-refresh) are captured. The `.dashboard-export-mode`/`.dashboard-export-sheet` CSS blocks are now dead but left in place (the `:not(.dashboard-export-mode)` selectors are still required). `node --check src/main.js` passes.
- **Dashboard PDF — KPI numbers / date header / bigger fonts / no Page-2 caption (2026-07-01)**: Four fixes to `window.exportDashboardPDF` ([src/main.js](src/main.js)) + CSS. (1) KPI tiles rendered FontAwesome spinners instead of numbers because the 120s `dashRefreshInterval` could fire mid-capture; now `clearInterval(dashRefreshInterval)` before capture (restarted in finally), and if a `.fa-spinner` is still present it `await window.fetchDashboardData()` then polls ≤5s for the spinner to clear. (2) Added a date header: a temporary `.dash-pdf-header` div is injected at the top of each page just before html2canvas capture and removed in finally — left "Clinic Snapshot Board" + per-page subtitle, right `ວັນທີ່ລາຍງານ:<range>` (from `#dashStartDate`/`#dashEndDate`) + `ພິມເມື່ອ:<dd/mm/yyyy hh:mm>`; new flat `.dash-pdf-header*` CSS in [src/style.css](src/style.css). (3) KPI text was too small in the 297mm capture — on-screen spread overrides bumped: value 2rem→3.1rem, title→13px/800, note→12.5px, tile min-height 110→128px. (4) Removed the Page-2 caption block ("PAGE 2 / Demographic & Community Snapshot / ເພີ່ມພື້ນທີ່…") from [public/partials/views/dashboard.html](public/partials/views/dashboard.html). Verified via Puppeteer harness [tmp/pdfs/dash-harness.html](tmp/pdfs/dash-harness.html)+[tmp/pdfs/dash-shot.cjs](tmp/pdfs/dash-shot.cjs) (served from repo root, captured at windowWidth 1485 / 297×210mm): both pages keep the 12-col grid, KPI 6/5/1/1 render large, date header present, no Page-2 caption. **Build note:** Vite app — if serving built `dist/` via `wrangler pages dev dist`, run `npm run build` to pick up changes (stale bundle keeps emitting the old `HIS_Dashboard_Landscape_Report.pdf` name; new code emits `HIS_Dashboard_YYYYMMDD.pdf`). `node --check src/main.js` passes.
- **Dashboard PDF — bulletproof KPI numbers + remove "Clinic Snapshot Board" (2026-07-01)**: Rendered PDF showed charts WITH data but KPI tiles STILL on spinners. Since `renderDashboardCharts` ([src/main.js](src/main.js)) sets KPI text (`#dash-total/new/old/inscorp`) BEFORE drawing charts in the same call, that combo only happens when a fresh `fetchDashboardData` reset the KPIs to spinners (line ~3937) and html2canvas captured before that call's chart re-render finished — a race the `.fa-spinner` wait-loop didn't close. Timing-proof fix: `renderDashboardCharts` now caches `window.__dashKpiCache = {total,newPatients,oldPatients,insCorp}`; `exportDashboardPDF` restores any tile still showing a spinner from that cache right before capture (`if(el.querySelector('.fa-spinner') && cache[key]!=null) el.textContent=cache[key]`). Also removed the literal "Clinic Snapshot Board" text from the injected `.dash-pdf-header` — the per-page Lao subtitle (ສະຫຼຸບການໃຫ້ບໍລິການ / ຂໍ້ມູນປະຊາກອນ ແລະ ຊຸມຊົນ) is now the header title, date meta on the right. Verified via [tmp/pdfs/dash-harness.html](tmp/pdfs/dash-harness.html): injected a spinner into Total-Visits with cache=6 → restore wrote "6" into the capture; header no longer shows the board name. `node --check src/main.js` passes. (Reminder: Vite app — rebuild `dist` if serving the built bundle.)
- **Dashboard PDF — page 2 charts empty (freeze re-renders during capture) (2026-07-01)**: PDF page 1 charts had data but ALL page 2 charts showed the on-canvas "ບໍ່ພົບຂໍ້ມູນ" empty state (drawn by the no-data Chart.js plugin at [src/main.js](src/main.js) ~3348 when a dataset is [0]). Since all 10 charts come from ONE `renderDashboardCharts` call, page1-data + page2-empty proves a SECOND render with empty/in-flight data ran between the two `html2canvas` page captures (user's range-click fetch or a late async resolve), clobbering charts after page 1 was already captured. Confirmed via a faithful Puppeteer harness with real Chart.js + the exact two-pass capture ([tmp/pdfs/dash-capture-harness.html](tmp/pdfs/dash-capture-harness.html)+[tmp/pdfs/dash-capture-shot.cjs](tmp/pdfs/dash-capture-shot.cjs)): with stable data page 2 captures perfectly, so the capture is fine — the cause is a stray re-render. Fix: freeze re-renders during capture — `renderDashboardCharts` early-returns when `window.__dashExporting` is true; `exportDashboardPDF` sets the flag after its own data load/KPI-restore and before sizing/capturing, clears it in `finally` then calls `fetchDashboardData()` once so the live view refreshes. Harness verified: firing a stray empty-render right after the page-1 capture is a no-op under the flag and page 2 still captures all charts with data. `node --check src/main.js` passes. (Vite app — rebuild `dist` if serving the built bundle.)
- **QR sticker — surname clipped on long Lao names (2026-07-04)**: User photo showed the ນາມສະກຸນ row on the printed patient sticker running past the sticker's right edge — the tail of `ເດັກຍິງ ນາງ ວັນນິດາ ປະພັດສະດາ` was sliced off. Root cause: `.pcard-row` in [src/style.css:5316](src/style.css#L5316) forces `white-space: nowrap; overflow: hidden; text-overflow: ellipsis` on every card row so the compact single-line layout stays clean, but the ຊື່ ແລະ ນາມສະກຸນ row concatenates Title + First + Last which can exceed the 180mm card width for Lao name compounds (child prefix like ເດັກຍິງ + given + long family name). Targeted fix: (1) added a modifier class `pcard-row-name` to all three name rows in [public/partials/print-areas.html](public/partials/print-areas.html) (pcard1/2/3), (2) `.pcard-name` font-size `38px → 32px` + `word-break: break-word; overflow-wrap: anywhere` in [src/style.css](src/style.css), (3) new `.pcard-row-name` rule sets `white-space: normal !important; overflow: visible !important; line-height: 1.15` so ONLY the name row wraps onto a 2nd line when needed — DOB / date / address / phone rows stay nowrap-truncated as before. Card height (89mm × 3 in the A4 stack) already has slack for the occasional 2-line name. Full detail in [docs/OPD_UI_REDESIGN.md](docs/OPD_UI_REDESIGN.md).
- **Dashboard — Revenue Groups→10 Services, Ins/Corp split into 2 charts, Occupation added, sidebar label→Dashboard (2026-07-04)**: Four dashboard tweaks in one round. (1) **"Revenue Groups" panel renamed** to `10 Services` (subtitle `ບໍລິການທັງໝົດ 10 ອັນດັບ`) in [public/partials/views/dashboard.html:79](public/partials/views/dashboard.html#L79). Data source (`v.Revenue_Group`) unchanged — the panel now displays the top-8 revenue-group tally under a service-oriented label. (2) **Ins/Corp split into two charts** — the combined KPI tile stays (`#dash-inscorp`), and two new compact panels added on page 2 between Age Group and Community Snapshot: `<h6>Insurance</h6>` (`chartInsurance`, `p.Insurance_Company`, top 5) and `<h6>Organization</h6>` (`chartOrganization`, `p.Name_Org`, top 5). (3) **Occupation chart added** — span-2 compact panel on page 2 (`chartOccupation`, `p.Occupation`, top 8, subtitle `ອາຊີບຄົນເຈັບ (Top 8)`). (4) **Sidebar label** — `nav.dashboard` translation in [src/main.js:147](src/main.js#L147) `ແຜງຄວບຄຸມ → Dashboard` (IPD entry `nav.ipdDashboard` left alone). **Data plumbing** in `renderDashboardCharts` at [src/main.js:4219](src/main.js#L4219): added `insurance/organization/occupation = {}` maps + a `Set` for per-patient dedup (returning patients would otherwise inflate their insurer/org/job by N visits and dwarf one-shot patients). Each visit contributes to those 3 maps only the first time its `Patient_ID` appears in the dataset; visit-scoped tallies (services, specialist, timeSlot) untouched. Reused the existing `insCorpHas()` helper (already filters ``, `-`, `ບໍ່ມີ`, `none`, `n/a`, `self pay`, `ຈ່າຍເອງ`) so blank / self-pay entries don't pollute Insurance/Organization. Chart IDs added to both `window.dashboardChartIds` (for the resize/relayout loop) and `compactDashboardCharts` (so their font sizes match the other compact tiles). `node --check src/main.js` passes; vite dev server (port 5173) starts clean with no console errors; all HTML checks (panel titles, canvas ids, subtitles) verified headlessly against the running server via [tmp/pdfs/dash-changes-check.cjs](tmp/pdfs/dash-changes-check.cjs). Full detail in [docs/OPD_UI_REDESIGN.md](docs/OPD_UI_REDESIGN.md).
- **Orgs page — only 6 rows visible after 405-row Excel import (2026-07-06)**: User imported `HIS_Organizations_Import_Template.xlsx` with 405 Dai-ichi customer rows via `handleOrgExcelUpload`; the Orgs table then showed only 6 Dai-ichi rows and complained the upload was broken. **Root cause: display, not upload.** Direct REST query (`GET /rest/v1/HIS_One_Organizations?Org_Code=eq.Dai-ichi` with `Prefer: count=exact`) returned `Content-Range: 0-404/405` — all 405 rows are in the DB, and total table size is 1,951 rows. `window.loadOrgs` in [src/main.js:11000](src/main.js#L11000) called `.select('*').limit(9999)` with no `.order()`. Supabase PostgREST silently caps a single response at the server `Max Rows` setting (default **1,000**) regardless of `.limit()`, so the client received an arbitrary 1,000-row slice; the Dai-ichi rows (inserted last, so late in physical order) mostly fell past that cap and only 6 survived. Client DataTable then sorted the 1,000-row slice by ORG CODE, floating those 6 Dai-ichi to page 1 — perfectly reproducing "6 uploaded" from the user's viewpoint. Fix: replaced the single query with a `.range(from, from+999)` pagination loop that keeps pulling 1,000-row pages until the server returns fewer than a page; also added `.order('Org_Code').order('Cus_ID_Ex')` for deterministic ordering. Verified against the live REST endpoint that Dai-ichi count is 405 and grand total is 1,951, so post-fix the DataTable footer should show the true 1,951. Full write-up in [docs/PATIENT_ORG_DROPDOWN_FIX.md](docs/PATIENT_ORG_DROPDOWN_FIX.md). **Warning for other `loadX` functions**: any table that can exceed 1,000 rows (Patients, Visits, Drugs_Master, Labs_Master, Vaccines_Master…) hits the same silent cap — `.limit(9999)` alone is not enough; use `.range()` pagination + explicit `.order()`.
- **Dashboard — Province + District fixed-bucket breakdown tables (2026-07-06)**: User supplied paper-reference screenshots (blue-header two-column tables: Province with rows `ວຽງຈັນ / ບໍລິຄຳໄຊ / ອື່ນໆ`, District with `Xaythany / Xaysettha / Chanthabuly / Naxaithong / Sikhottabong / others`) and asked for the same on the Dashboard. Added two new `<article class="dashboard-report-panel">` panels after the Occupation panel in [public/partials/views/dashboard.html](public/partials/views/dashboard.html) with `<table class="dashboard-breakdown-table">` bodies `#tblProvinceBody` / `#tblDistrictBody`. New `.dashboard-breakdown-wrap` / `.dashboard-breakdown-table` CSS block in [src/style.css](src/style.css) — flat clinical look per `[feedback_no_ai_looking_ui]`: solid `#1B6BB0` header, white text, 4px radius, `#f7fafc` alternating row, no gradients/shadows; `.dashboard-breakdown-count` is right-aligned tabular-nums. New `window.renderProvinceDistrictTables(visits)` in [src/main.js](src/main.js), called at the end of `renderDashboardCharts` just before `refreshDashboardChartLayout()`. Aggregation is **patient-level dedup** via a `Set` on `Patients.Patient_ID` (returning patients don't inflate their home province/district). Each patient's Province/District is normalized (`trim`+`toLowerCase`), and matched via `.includes()` against per-bucket alias arrays — Province: `ວຽງຈັນ`↔`vientiane|ນະຄອນຫຼວງ|nakhon luang|nakhonluang`, `ບໍລິຄຳໄຊ`↔`bolikhamxay|borikhamxay|bolikhamsai|borikhamsai`. District: each of the 5 named districts carries Lao and common English-spelling aliases. Unmatched → `ອື່ນໆ` (province) / `others` (district). i18n note: panel titles + `<th>` labels auto-translate (Lao mode shows `ແຂວງ`/`ເມືອງ`; English mode `Province`/`District`); row labels stay in their original language. Verified in the live vite dev preview by feeding 9 synthetic patients through `window.renderProvinceDistrictTables` — counts and dedup both correct — and by `preview_inspect` on the header/data cells (background `rgb(27,107,176)` = `#1B6BB0`, white text, `font-weight:700`; count cell right-aligned, `color: rgb(14,59,95)`). Full write-up in [docs/OPD_UI_REDESIGN.md](docs/OPD_UI_REDESIGN.md).
- **Dashboard — restructure Province + District into single 2-column band; drop "Top 5 districts" chart (2026-07-06)**: Followup to the same-day Province+District tables. User asked to (a) remove the "Top 5 ເມືອງທີ່ມາໃຊ້ບໍລິການ" bar chart (canvas `#chartProvince`) from Community Snapshot, and (b) put the two tables into a single side-by-side band styled like Community Snapshot. In [public/partials/views/dashboard.html](public/partials/views/dashboard.html) replaced the two separate Province/District panels + the old Community Snapshot with **one** `dashboard-report-panel--span-4 dashboard-report-panel--community` panel titled "Province & District" (subtitle `ຈຳແນກຕາມແຂວງ ແລະ ເມືອງ`). Inside sits a `dashboard-report-split` grid reusing the same `dashboard-report-split-card--map` / `--doctor` slot classes as before (icon `fa-map-marker-alt` for Province, `fa-city` for District); each slot wraps the `.dashboard-breakdown-wrap`/`.dashboard-breakdown-table` from the earlier commit. Top 5 Doctors preserved as its own compact `dashboard-report-panel--span-2 --accent` panel with the existing `#chartMarketing` canvas. In [src/main.js](src/main.js) removed `chartProvince` from `window.dashboardChartIds` and `compactDashboardCharts`, dropped the `topDist = getTopNWithOthers(district, 5, 0.001)` calc and the `createChart('chartProvince', …)` call — the district-count map is still built by the visits loop but no longer read (harmless). Verified in the live vite preview at 1440×900: split cards render side-by-side (615×N each, 14px gap, same Y), Province rows `ວຽງຈັນ/ບໍລິຄຳໄຊ/ອື່ນໆ` + District rows `Xaythany/Xaysettha/Chanthabuly/Naxaithong/Sikhottabong/others` populate correctly, `#chartProvince` canvas is gone, `#chartMarketing` still present.
- **Dashboard — polish Province/District breakdown cards + drop scrollbar (2026-07-06)**: Followup polish pass. User photo showed the District card had a visible right-side scrollbar (Xaythany row hidden) and asked for a nicer "frame" look. In [src/style.css](src/style.css) `.dashboard-breakdown-*` block: removed `max-height:170px` from `.dashboard-breakdown-wrap` (was truncating the 6th district row) and set `overflow:hidden` on the wrap; bumped table font 12.5→13px; row padding 5px 10px → 9px 14px, header padding 6px 10px → 9px 14px + 0.2px letter-spacing; softened border color `#e2e8f0 → #d6e0ea`, zebra `#f7fafc → #f5f9fc`; added `tr:hover td { background:#eaf2fa }` and a bold **totals footer** row (`tfoot td` bg `#f0f6fb`, 2px `#1B6BB0` top border, dark-blue text) whose second cell right-aligns automatically; added `.dashboard-breakdown-empty` italic center placeholder for zero-data. In [public/partials/views/dashboard.html](public/partials/views/dashboard.html) each of the two tables now has `<tfoot><tr><td>ລວມ / Total</td><td id="tblProvinceTotal|tblDistrictTotal">0</td></tr></tfoot>`. In [src/main.js](src/main.js) `paint()` inside `renderProvinceDistrictTables` now takes a `totalId`, sums the bucket values, renders `<tr class="dashboard-breakdown-empty"><td colspan=2>ບໍ່ພົບຂໍ້ມູນ</td></tr>` when total=0, and writes the sum to the tfoot cell. Verified in the live vite preview (1440×900) with 9 synthetic patients: Province total = 9 (3 rows), District total = 9 (6 rows all visible, `distOverflow:false`); computed styles show tfoot bg `rgb(240, 246, 251)`, dark-blue text, weight 700; wrap height 189px matching table 187px so nothing scrolls.
- **Dashboard — swap Province/District tables for bar charts (2026-07-06)**: User asked "ບໍ່ເຫັນສິເປັນ chart ໂຊ" ("wouldn't it be better as a chart?"). Replaced the two breakdown tables with `<canvas id="chartProvinceBreakdown">` / `<canvas id="chartDistrictBreakdown">` inside the same `dashboard-report-chart--split` box that `chartProvince` (now removed) used to live in — [public/partials/views/dashboard.html](public/partials/views/dashboard.html). The `Total` moved from a table `<tfoot>` row to a pill in the split-title (`.dashboard-breakdown-total` — bg `#eaf2fa`, dark-blue text, weight 800, tabular-nums, 999px pill; new CSS in [src/style.css](src/style.css) next to `.dashboard-report-split-title`). Purged the no-longer-used `.dashboard-breakdown-wrap` / `.dashboard-breakdown-table*` / `.dashboard-breakdown-count` / `.dashboard-breakdown-empty` block (about 55 lines) from style.css since HTML no longer references them. In [src/main.js](src/main.js) `renderProvinceDistrictTables` swapped `paint()` for `draw()` that computes the same totals + calls `window.createChart(canvasId, 'bar', labels, values, palette, true)` (the shared compact-bar renderer used by Insurance/Organization/Occupation). Added `chartProvinceBreakdown` and `chartDistrictBreakdown` to both `window.dashboardChartIds` and the `compactDashboardCharts` Set so the resize loop + compact font sizing pick them up. **Design note**: the buckets are fixed lists (2+`ອື່ນໆ` for Province, 5+`others` for District), so zero-count bars are still drawn — categories stay stable across date ranges, which is important for a comparison view. Verified via `Chart.getChart(canvas)` in the live vite preview: 11 synthetic patients aggregated to `ວຽງຈັນ 7 / ບໍລິຄຳໄຊ 2 / ອື່ນໆ 2` and `Xaythany 4 / Xaysettha 0 / Chanthabuly 2 / Naxaithong 0 / Sikhottabong 0 / others 5`; total pill shows `11` on both cards; canvas rendered at 589×156. Full details in [docs/OPD_UI_REDESIGN.md](docs/OPD_UI_REDESIGN.md).
- **Dashboard PDF — fit exactly 2 A4 pages (2026-07-06)**: User PDF (`Dashboard.pdf`) showed page 2 clipping the Province & District band mid-chart and pushing Top 5 Doctors onto a non-existent page 3. **Root cause**: the `dashboard-report-grid--spread-detail` grid is 12 cols (see [src/style.css:6751](src/style.css#L6751)) and every default detail-panel gets `grid-column: span 3` at [src/style.css:6772](src/style.css#L6772). Because Top 5 Doctors was declared **after** the `--span-4` (=span-12) Province & District panel in [public/partials/views/dashboard.html](public/partials/views/dashboard.html), auto-placement floated Doctors onto Row 4 (row 2 already had Insurance + Organization + Occupation = 9 cols, and P&D can't fit in the last 3, so it wraps to Row 3, leaving Doctors on Row 4). Fix (a): **reordered** — moved Top 5 Doctors before Province & District in the HTML source, so it fills Row 2 slot 10-12; P&D now sits alone on Row 3 = 3 rows total. Fix (b): shaved chart heights so 3 detail rows + summary rows fit inside 210mm exactly — page 2 `--compact 172→148px`, `--split 156→130px`, `--split-card padding 12→10px 12px 8px`, grid `gap 14→10px`, `panel-head margin 10→6px`; page 1 `--hero 214→206px`, `--medium 174→168px`. Verified via the export-simulation loop: injected the same `.dash-pdf-header` and forced each `.dashboard-report-page--spread` to 297×210mm with `overflow:hidden`; `scrollHeight === clientHeight === 792px` on both pages → **0 overflow**, exactly 2 pages. Full write-up in [docs/OPD_UI_REDESIGN.md](docs/OPD_UI_REDESIGN.md).

- **Dashboard on-screen font bump (2026-07-06)**: The A4/PDF-export mode used deliberately small fonts (h6 12px, subtitle 10px, KPI title 13px, chart ticks 8px) so two full pages of KPIs + charts fit on landscape A4. On the actual screen those same rules also applied because the panels live under `#dashboardPrintArea.dashboard-a4-sheet:not(.dashboard-export-mode)`; the user complained everything looked cramped ("ຊ່ວຍປັບ font ໃຫ້ໃຫຍ່ຂື້ນຕື່ມ"). Bumped **on-screen only** (export CSS untouched): panel-head h6 12→15px, panel-head p / split-title 10→12.5px, KPI title 13→15px, KPI note 12.5→14px, KPI value 3.1→3.4rem in [src/style.css:6690](src/style.css#L6690). Also raised the Chart.js compact-dashboard font sizes in [src/main.js:4126](src/main.js#L4126) (legend 8→11, tickFontSize 8→11, yTickFontSize 9→12, dataLabelSize 8→11) and the empty-state overlay in [src/main.js:3347](src/main.js#L3347) (message 13→15px, sub 11→13px). Toolbar subtitle inline style bumped 11.5→13px in [public/partials/views/dashboard.html:6](public/partials/views/dashboard.html#L6). PDF export mode untouched — verified the exclusive `.dashboard-export-mode` and `.dashboard-export-sheet` selector chains at [src/style.css:5895](src/style.css#L5895) and [src/style.css:5996](src/style.css#L5996) so the A4 report still packs at the original sizes.

- **Dashboard bar-chart datalabels — 7+ items were silently hidden (2026-07-06)**: The "Occupation" chart (Top 8) rendered 7 bars with no numeric labels while every other chart on the same spread showed them. Root cause in [src/main.js:4157](src/main.js#L4157): the datalabels `display` gate for compact dashboard charts is `safeData.length <= (isHorizontal ? 6 : 5)`, a soft-limit added earlier to prevent label overlap. Occupation is horizontal with `slice(0,8)` upstream, so any result with 7–8 distinct occupations tripped the limit and dropped ALL labels on that chart. Raised the ceiling to `isHorizontal ? 12 : 8` — Top-8/Top-10 horizontal panels (Occupation, Insurance, Organization, Doctors, Province, District) now always show labels, and vertical charts up to 8 items (Age Group, Time Slot are ≤4 so no effect) are covered too. Chose 12 rather than removing the cap so future ad-hoc "Top 20" additions still self-limit. No change to legend/font logic — just the display predicate.

- **OPD Card printed stale patient age (2026-07-06)**: Reviewer flagged DOB 2023-07-20 printing as Age 1 on today (2026-07-06) — real age 2y 11m. Bug in [src/main.js:9211](src/main.js#L9211) and [src/main.js:9290](src/main.js#L9290): `safeSetText("popd_age", d.Age)` printed the `Patients.Age` snapshot column, which is populated at registration by `calculateAgeForm()` and never refreshed. Child registered mid-2024 at age 1 kept `Age = 1` in the DB. Added a `printOPDCard`-local `ageFromDob(dob)` helper using calendar arithmetic (`getFullYear/getMonth/getDate` with birthday-not-yet-reached decrement) and a `printAge` value derived once, used for both Page 1 (`popd_age`) and Page 2 (`popd2_age`). Falls back to `d.Age` only when `Date_of_Birth` is missing/unparseable; future-DOB data errors yield empty string → fallback. `calculateAgeForm()` (registration path) untouched — filters and reports elsewhere still use the stored snapshot. See [docs/OPD_UI_REDESIGN.md](docs/OPD_UI_REDESIGN.md#2026-07-06--opd-card-printed-a-stale-patient-age).

- **Age snapshot leaked into every visit list (2026-07-06 follow-up)**: OPD Card now shows live age (previous fix), but the Triage list still rendered `3 ປີ` for a DOB 2023-07-20 patient where the OPD Card correctly showed `2`. Root cause: `_fetchTriageQueue` at [src/main.js:6752](src/main.js#L6752) and `_fetchOpdQueue` at [src/main.js:6931](src/main.js#L6931) both mapped `age: r.Age || p?.Age || 0` — the stored `Visits.Age` snapshot (set at visit creation) or `Patients.Age` snapshot (set at registration). Same stale-snapshot bug, different surface. Reports list at [src/main.js:4854](src/main.js#L4854) had the identical pattern for `p.Age`.

Extracted a global `window.ageFromDob(dob)` helper next to `calculateAgeForm` at [src/main.js:2985](src/main.js#L2985) using calendar arithmetic (returns `null` when DOB missing/invalid/future so callers can fall back). Refactored the OPD Card `printOPDCard` local helper to call it. Added `Date_of_Birth` to the two patient sub-selects (Patients join for both queues) at [src/main.js:6640](src/main.js#L6640) and [src/main.js:6853](src/main.js#L6853). All three age-display sites now read `window.ageFromDob(p?.Date_of_Birth || r.Date_of_Birth) ?? (r.Age || p?.Age || 0)` — DOB is source of truth, snapshots are the fallback. `calculateAgeForm()` was also rewritten on top of the helper so registration writes a consistent value (previously used the buggy `getUTCFullYear() - 1970` epoch-diff trick which was accidentally correct for this test case but fragile around leap seconds).

NOT touched: analytics/reports that filter by `Age_Group` still key off the stored `Patients.Age_Group` band, since re-binding a pediatric visit into a new band mid-report would break the historical accuracy of the record. Only the visible "current age" columns are live.

- **Doctor dropdown moved from EMR to Triage; Top 5 Doctors gate relaxed (2026-07-06)**: The dashboard "Top 5 Doctors" (chartMarketing) was consistently empty because `Doctor_Name` was only assigned at the EMR stage, and additionally the counter at [src/main.js:4337](src/main.js#L4337) required `Mapped_Specialist` to be non-empty (a heuristic to filter nurses out when doctors were entered as free-text). User wanted the assignment at Triage time so it shows up immediately.

Changes:
- Added `<select class="form-select dyn-Doctor" id="v_doctor" name="v_doctor">` to [public/partials/modals/triage-modal.html](public/partials/modals/triage-modal.html) — the `.dyn-Doctor` class is auto-populated by `loadMasterDataGlobalCallback` at [src/main.js:10697](src/main.js#L10697) from the `MasterData["Doctor"]` category, same source the EMR modal uses.
- Added `doctor: r.Doctor_Name || ""` to the `_fetchTriageQueue` row mapping at [src/main.js:6752](src/main.js#L6752) so pre-existing values round-trip when re-opening a triage row.
- `openTriage` at [src/main.js:7116](src/main.js#L7116) now hydrates `#v_doctor` (with a safety option-append for unknown values, matching the recorded-by pattern).
- `executeTriageSave` writes `Doctor_Name: fd.v_doctor` on both the primary and fallback Visits update payloads, and caches it into `currentTriageData[savedIdx].doctor` so the local list stays in sync until the next `loadTriageQueue()`.
- Dashboard doctor counter no longer requires a specialist mapping: with the dropdown backed by curated MasterData the nurse-leakage risk is gone. Kept the `docName !== "ບໍ່ລະບຸຊື່ແພດ"` placeholder guard.

Not touched: the EMR modal still has its own `#emrDoctor` dropdown so that a doctor at the EMR stage can override the triage assignment (e.g. reassignment mid-visit). Both dropdowns write to the same `Visits.Doctor_Name` column, EMR write always wins because it happens later in the flow.

- **Triage doctor dropdown showed non-doctors (2026-07-06)**: The Triage `#v_doctor` select initially used class `dyn-Doctor`, which reads from `MasterData["Doctor"]`. That category had been used as a general staff list — nurses and other roles were mixed in, so the dropdown showed everyone. Root cause is data pollution, but the correct source of truth for staff roles is `HIS_One_Users.Role`, not MasterData.

Fix: added `window.refreshDoctorUserList()` + `window.applyDoctorUserSelects()` and `window.doctorUsersCache` near [src/main.js:5382](src/main.js#L5382). The refresh queries `Users` filtered by `Role="doctor"` (and drops `Status="inactive"`), caches Name-strings, then repopulates every `<select class="doctor-users-select">` in the DOM. Called during login init (added to the existing `Promise.all` at [src/main.js:3397](src/main.js#L3397)) and every time `loadUsers()` runs — so the admin add/edit/delete flow refreshes it automatically.

Switched `#v_doctor` in [public/partials/modals/triage-modal.html](public/partials/modals/triage-modal.html) from `dyn-Doctor` to `doctor-users-select`. `openTriage` now calls `applyDoctorUserSelects()` before hydrating so late-cache scenarios are covered, then keeps the "append unknown value" safety pattern so an old visit whose `Doctor_Name` is not currently in the active-users list still renders (and remains editable).

Backwards compat: if `refreshDoctorUserList` fails or returns 0 rows (empty Users table), `applyDoctorUserSelects` short-circuits and leaves whatever the initial `-- ເລືອກແພດ --` placeholder had. Not a fallback to MasterData by design — new installs should register doctors as Users with role=doctor.

Not touched: EMR modal `#emrDoctor` still uses `dyn-Doctor` (MasterData). The user complained about Triage specifically; changing EMR would silently drop any doctor listed only in MasterData and not in Users, which we cannot verify from here. Revisit if the same complaint comes up on the EMR side.

- **Triage doctor dropdown was empty when no HIS_One_Users had role=doctor (2026-07-06)**: Previous change made the Triage `#v_doctor` source `HIS_One_Users` filtered by `Role="doctor"`. This system has doctors listed in `MasterData["Doctor"]` but has not yet added them as Users, so the dropdown showed only the "-- ເລືອກແພດ --" placeholder. `applyDoctorUserSelects` at [src/main.js:5382](src/main.js#L5382) now falls back to `masterDataStore["Doctor"]` when the users cache is empty (writes `data-doctor-source="users"` or `"masterdata"` on each select so the source is inspectable). Also hooked `applyDoctorUserSelects()` into the tail of `loadMasterDataGlobalCallback` so the fallback fires as soon as MasterData is loaded, not just at the initial login sequence. Net behavior: registered doctors in Users win; otherwise the existing curated MasterData list shows up — same behavior the user had before the users-based filter went in. Follow-up when the "non-doctor pollution" complaint returns: register the real doctors as Users with role=doctor, and this dropdown flips to the filtered list automatically without a code change.

- **Settings page — removed obsolete OPD Header/Footer URL inputs (2026-07-06)**: The System Settings card at [public/partials/views/settings.html](public/partials/views/settings.html) still surfaced free-text `OPD Header URL` and `OPD Footer URL` inputs, but the "OPD Print Settings" card below already provides Page 1/2 Header + Footer image upload cards that write to `opd_print_page1_header_image` etc. and are what `printOPDCard` prefers at [src/main.js:9240](src/main.js#L9240). The two URL fields were leftover duplicates. Cut per user request ("ຕັດ 2 ສ່ວນນີ້ອອກ").

Removed: the two `<div class="col-md-6">` rows in settings.html, plus every `$("#setOpdHeaderUrl")` / `$("#setOpdFooterUrl")` reference in `submitSettingsForm`, the initial post-login settings hydrate, and `loadSettings()`. Also dropped `OpdHeaderUrl` / `OpdFooterUrl` from the upsert payload and the `systemSettings` spread inside `submitSettingsForm`.

Deliberately kept: the Settings-table read paths that copy `OpdHeaderUrl` / `OpdFooterUrl` into `systemSettings.opdHeaderUrl` / `.opdFooterUrl`, and the print-time fallback chain `opdPrintPage1HeaderImage || opdHeaderUrl || logoUrl`. Any tenant with legacy Header/Footer URLs already saved in the Settings table still gets them honored on print — the values just can	 be edited from this page anymore. If they need a change, admins can either upload a new image via the card below, or update the row directly via master data / DB.

- **OPD print header/footer images did not persist after upload (2026-07-06)**: User complained the four image slots on Settings never showed anything even after they had uploaded ("ຍັງບໍ່ອອກ", selected "OPD Print header/footer images"). Load path at [src/main.js:3598](src/main.js#L3598) and `renderOpdPrintImagePreview` both looked correct; root cause was UX-side. `handleOpdPrintImageUpload` at [src/main.js:5893](src/main.js#L5893) only wrote the returned public URL into the hidden input; nothing was saved to the Settings table until the user separately clicked the "Save OPD Print Settings" button at the bottom of the card. Users uploaded and navigated away, and the URL was lost.

Fix: added `window.persistOpdPrintImageSetting(settingKey, value)` that upserts the single row into `Settings` (Key/Value) and mirrors the value into `systemSettings.opdPrintPage*Image` so subsequent OPD prints in the same session pick it up without a full reload. `handleOpdPrintImageUpload` awaits it right after the upload succeeds. `clearOpdPrintImage` also persists an empty string so removing an image is instantly durable. The bulk "Save OPD Print Settings" button remains (harmless double-write) so any workflow that expects it still works.

Bonus: any Settings-table upsert error now surfaces via Swal instead of a silent console.error. If Key isn	 configured as a unique constraint on the tenant\s Settings table, the user will see the error immediately instead of losing uploads silently.

- **Settings view — both configuration cards removed per user request (2026-07-06)**: User confirmed via AskUserQuestion that both cards should be deleted entirely, not just the URL fields. Cut two cards from [public/partials/views/settings.html](public/partials/views/settings.html):
  1. `ຕັ້ງຄ່າລະບົບ` (Hospital Name, Logo URL, remember-module toggle, Save).
  2. `ຕັ້ງຄ່າໃບ OPD / OPD Print Settings` (four image dropzones + Save).
  Only the Master Data section remains under `#view-settings`.

Deleted dead JS in [src/main.js](src/main.js): `submitSettingsForm`, `submitOpdPrintSettingsForm`, `loadSettingsData` (older duplicate load path), `getOpdPrintImageFieldIds`, `renderOpdPrintImagePreview`, `clearOpdPrintImage`, `uploadOpdPrintImage`, `handleOpdPrintImageUpload`, `persistOpdPrintImageSetting`. Simplified the on-navigate-to-settings handler at [src/main.js:3598](src/main.js#L3598) to just call `renderMasterCategoryUI()` and `loadMasterList()` — no more form hydration.

Also deleted the now-unused `.opd-print-image-card`, `.opd-print-image-dropzone`, `.opd-print-image-placeholder` blocks from [src/style.css](src/style.css).

Kept intact: the post-login Settings fetch at [src/main.js:3402](src/main.js#L3402) that populates `systemSettings.hospitalName`, `.logoUrl`, `.opdHeaderUrl`, `.opdFooterUrl`, `.opdPrintPage*Image`, `.rememberLastModule`. `printOPDCard` still uses `opdPrintPage1HeaderImage || opdHeaderUrl || logoUrl` at [src/main.js:9099](src/main.js#L9099), so any tenant with existing header/footer/logo rows in the Settings table continues to get them on printed OPD Cards. To change these values now, admins update the Supabase Settings table directly (Key column: HospitalName, LogoUrl, opd_print_page1_header_image, etc.) — no UI path.

- **Triage form — Type, Recorded By, Doctor now required (2026-07-06)**: User requested three additional required fields in the ຊັກປະຫວັດ modal. Added `required` attribute and a red `<span class="text-danger">*</span>` on each label in [public/partials/modals/triage-modal.html](public/partials/modals/triage-modal.html) for `#v_type`, `#v_recorded_by`, `#v_doctor`. Also added a leading `-- ເລືອກປະເພດ --` empty option to `#v_type` (previously the only option was `OPD` so the field was implicitly always filled). Backed by JS validation in `submitTriageForm` at [src/main.js:6237](src/main.js#L6237): mirrors the existing Department check and shows a Swal warning (`ຂໍ້ມູນບໍ່ຄົບ`) with a Lao message per field. Native HTML5 `required` handles the majority case; Swal fires when the form is submitted via the "ບັນທຶກ ແລະ ສົ່ງເຂົ້າຫ້ອງກວດ" button after a browser bypass. `openTriage` untouched — hydrating an existing row where `r.type` is empty will now leave the Type dropdown on the placeholder, forcing the user to pick before saving; that is the intended behavior for a required field.

- **Triage list — new OPD/IPD column (2026-07-06)**: Added a column between ສະຖານະ and ຈັດການ that renders the visit `Visit_Type` (already exposed as `r.type` by `_fetchTriageQueue` at [src/main.js:6752](src/main.js#L6752)). New `<th class="text-center">OPD/IPD</th>` in [public/partials/views/triage.html](public/partials/views/triage.html); row template in [src/main.js:6928](src/main.js#L6928) renders a pill: `bg-primary` for OPD, `bg-info` for IPD, `bg-secondary` for any other non-empty value, and a muted dash when the field is blank. Bumped the loading spinner colspan from 6 to 7 to match. No fetch changes needed — Visit_Type is already selected as part of the visit row and joined into `r.type`.

- **Triage list OPD/IPD column was always dashed (2026-07-06 same-day follow-up)**: The new column landed but every row rendered `-`. Root cause: `_fetchTriageQueue` at [src/main.js:6680](src/main.js#L6680) never exposed `Visit_Type` on the row shape (I had added `doctor` in an earlier pass but missed `type`). Row template read `r.type` which was always `undefined`. Added `type: r.Visit_Type || ""` (and `site: r.Site || ""` while there, since it round-trips through the same modal). Old rows saved before Type was required stay dashed (their DB `Visit_Type` is null); new triage saves — now that Type is required — will render as a pill. Consistent with the OPD queue path at [src/main.js:4823](src/main.js#L4823) which uses the same convention.

- **Triage Type dropdown was pulling from MasterData (departments, not OPD/IPD) — 2026-07-06**: The new OPD/IPD column rendered "ຫ້ອງກວດ" pills for most rows. Root cause was `handleTriageSiteChange` at [src/main.js:8741](src/main.js#L8741): it emptied `#v_type` and refilled it from `masterDataStore["PatientType_InSite"]` (or `PatientType_Onsite`) which the tenant had populated with Lao department strings. Triage save then wrote those strings into `Visits.Visit_Type`. Same pattern lives in the EMR modal via `handleSiteChange` on `#emrDeptType` — intentionally left alone since the user only complained about Triage.

Fix: gutted `handleTriageSiteChange` to a no-op (kept the export so the `onchange="handleTriageSiteChange()"` attribute in the modal doesn	 404); hard-coded `#v_type` in [public/partials/modals/triage-modal.html](public/partials/modals/triage-modal.html) to just OPD + IPD; row template in [src/main.js:6929](src/main.js#L6929) now falls back to a muted dash for anything that isn	 OPD/IPD instead of rendering the raw legacy string as a `bg-secondary` pill; `openTriage` at [src/main.js:7100](src/main.js#L7100) no longer appends unknown `r.type` values as a fresh option — legacy junk lands on the placeholder, forcing the user to pick a valid value before saving.

Going forward, Visits.Visit_Type will only ever get "OPD" or "IPD" written from Triage. Historical rows keep their existing values in the DB but display as dash until re-saved.

- **OPD queue — newest arrivals now sort to the top (2026-07-06)**: User asked `ຕາຕະລາງ OPD ໃຫ້ລຽງຄົນເຈັບຄົນທີ່ເຂົ້າມາໃໝ່ຂື້ນເທີງ`. `_fetchOpdQueue` was already querying Supabase with `.order('Date', { ascending: false })`, but the ordering was lost by the `Object.values(visitById)` dedup pass at [src/main.js:6786](src/main.js#L6786), and DataTable in `loadQueue` was init'd with no `order` option so it fell back to alphabetical sort of the locale-formatted `dd/MM/yyyy HH:mm` string — which does not equal newest-first. Fix: sort the queue array by `rawDate` desc in `loadQueue` right after the `myRoom` filter (before writing to `queueDataStore`), so the row indices used by `openEMR(i)` / `viewEMR(i)` / `printOPDCard('opd', i)` still line up with the rendered order. DataTable init unchanged — user can still click column headers to re-sort.

- **OPD queue sort — DataTable was clobbering the JS sort (2026-07-06 same-day follow-up)**: The `q.sort()` by `rawDate` added earlier did run, but the OPD table still showed oldest-first because `$('#queueTable').DataTable({...})` was init'd without an `order` option, so DataTable applied its default sort (first column ascending) which alphabetised the locale `dd/MM/yyyy HH:mm` strings — for a same-day queue this is exactly time-ascending. Fix: add `order: []` to the DataTable options in `loadQueue` at [src/main.js:8654](src/main.js#L8654) so DataTable preserves the DOM order the sort produced. Clicking a column header still lets the user override.

- **Backup workflow was reporting success while uploading nothing (2026-07-06)**: User showed the Backup page: latest run marked ✅ success, but "Supabase Storage" table shows `ບໍ່ມີໄຟລ໌ backup ໃນ bucket his-backups` and "Google Drive" shows `ຍັງບໍ່ໄດ້ຕັ້ງຄ່າ Google Drive`. Root cause on Supabase side: `backup_rest.py` at [backup/scripts/backup_rest.py:12](backup/scripts/backup_rest.py#L12) reads `SUPABASE_STORAGE_BUCKET` with `os.environ.get(..., "")` — and the upload block at [line 251](backup/scripts/backup_rest.py#L251) prints "SKIPPED (SUPABASE_STORAGE_BUCKET not set)" and moves on. Since `main()` only failed when a **table** export failed, the exit code was 0 → GitHub Actions marks the run green even though no ZIP was uploaded. Result: UI shows a success run next to an empty bucket, which looks broken but is technically consistent.

Fix: added a fail-fast check at the top of `main()` in [backup/scripts/backup_rest.py](backup/scripts/backup_rest.py) that `sys.exit(2)` when `SUPABASE_BUCKET` is empty, with a message pointing at the missing GitHub secret. Next scheduled or manual run will fail visibly instead of silently, and the "ປະຫວັດ Backup" table will show a red run — much easier to debug than "success with no artifact".

Config side (user action, not code): to actually enable auto-backup the user must set two/four GitHub Actions secrets on `ITLXH/HIS-LXH-SYSTEM`:
  - `SUPABASE_STORAGE_BUCKET` — bucket name, must match Cloudflare Pages env of same name (default `his-backups` per [functions/api/backup/list.js:10](functions/api/backup/list.js#L10)).
  - `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` — should already be set since prior runs completed the CSV export step.
  - `GOOGLE_SERVICE_ACCOUNT_JSON` + `GOOGLE_DRIVE_FOLDER_ID` — optional; without these the Google Drive step in [.github/workflows/supabase-backup.yml:60](.github/workflows/supabase-backup.yml#L60) skips (this is correct behaviour — the UI showing "not configured" is honest).

- **OPD / IPD file cover pages (ໜ້າປົກ) added (2026-07-07)**: User dropped `~/Downloads/ໜ້າປົກ IPD.docx` and `~/Downloads/ໜ້າປົກ OPD CARD.docx` and asked for a printable file-cover page with the Luckxay logo + LXH code. Source docx used "One MEDs Clinic" banner and a plain A4 layout with big "OPD CARD" / "IPD CHART" title, corner IDs, and patient title/name — reimplemented in the app so covers can be generated for any patient from the OPD queue or IPD inpatient list, not just the two mock samples.

Implementation:
  - New print template `#cover-print-area` at the end of [public/partials/print-areas.html](public/partials/print-areas.html): header (`/luckxay-logo.jpg` + "ໂຮງໝໍ ຫຼັກໄຊ / LUCKXAY HOSPITAL"), corner strip (left = ຫ້ອງກວດ for OPD or IPD No for IPD, right = HN + Code128 barcode via existing `renderOpdPatientBarcode('cover_barcode')`), big centered title (`OPD CARD` or `IPD CHART`), patient title + Lao name + English name, and a date/ward footer.
  - CSS block `#cover-print-area .cover-page` in [src/style.css](src/style.css) — A4 portrait, 198mm × 289mm, Noto Sans Lao stack, print-optimized. Title uses `#1B6BB0` (Luckxay blue) so it matches sidebar/print header treatment already in use.
  - JS in [src/main.js](src/main.js): `window.printOPDCoverFromQueue(i)`, `window.printIPDCoverFromAdmission(admissionId)`, dispatcher `window._printCoverPage({kind, patientId, department, admissionId, dateStr})` that fetches the patient row from Supabase and fills the template, and `window.exportCoverPageAsPdf(suffix, prefix)` that html2canvas → jsPDF the sheet as a single A4 page (same pattern as `exportOpdCardAsPdf`).
  - Buttons in the ຈັດການ column: `loadQueue` OPD rows get `<i class="fas fa-file-alt"></i> ໜ້າປົກ` next to the existing ພິມ button (all three status branches: Waiting OPD, Waiting Lab, Completed); IPD `renderIpdInpatientTable` adds a ໜ້າປົກ button in both the active-admission and discharged action groups.

Cover output verified via preview_eval: injected the partial into DOM, filled in fake OPD + IPD data, rendered — logo shows, barcode renders, big blue title and patient name look clean. Actual PDF export path is exportCoverPageAsPdf which mirrors `exportOpdCardAsPdf` at [src/main.js:9309](src/main.js#L9309) so any browser that already prints the OPD Card will produce the cover the same way.

- **IPD cover title renamed CHART → CARD (2026-07-07)**: User note "IPD Card ບໍ່ແມ່ນ chart" — the inpatient cover was labelled `IPD CHART` but should be `IPD CARD` to stay parallel with `OPD CARD`. Updated the title string in [src/main.js:9543](src/main.js#L9543) (`setText('cover_title', isIPD ? 'IPD CARD' : 'OPD CARD')`), the chooser dialog `denyButtonText` at [src/main.js:9480](src/main.js#L9480), plus the file-header comments in [src/main.js:9457](src/main.js#L9457), [public/partials/print-areas.html:275](public/partials/print-areas.html#L275), [src/style.css:2610](src/style.css#L2610), and the docs table in [docs/OPD_IPD_COVER_PAGES.md](docs/OPD_IPD_COVER_PAGES.md). Subtitle `ໃບບັນທຶກຄົນເຈັບໃນ` and red accent are unchanged.

- **Cover PDF drops Lao tone marks — attempted fix caused hang, reverted (2026-07-07)**: Comment from ອ້າຍໂນ່ — patient names containing ໄມ້ເອກ ( ່ ) rendered on-screen but were missing on the exported cover PDF. First attempt: added `foreignObjectRendering: true` to `exportCoverPageAsPdf` html2canvas call so text would go through SVG `<foreignObject>` and use the browser's native shaping engine. This works in theory but **hangs indefinitely in production** — html2canvas 1.4.1 tries to inline the Google Fonts `@font-face` rules into the foreignObject and the WOFF2 fetch fails CORS inside the data-URI SVG context, then the library waits forever for fonts to load. User hit "This page isn't responding" on `/patients`. Reverted to the plain canvas render at [src/main.js:9655](src/main.js#L9655) — un-hangs the app but the tone-mark drop remains. Also **removed `letterRendering: true`** (which was there before) — per-character rendering desynchronizes combining marks from their base consonant, so keeping it off can only help. CSS line-height bump on `.cover-field-value` (1.2 → 1.5 + 2mm top padding) at [src/style.css:2773](src/style.css#L2773) is kept as defense-in-depth for any font that needs headroom above the ascender. **Still open:** cover PDF may still drop tone marks. Next attempts to try: (a) pre-render the Lao name to an inline SVG via canvas 2D `measureText` + `fillText` and inject that as an `<img>` before html2canvas runs, or (b) switch this one export path to `pdf-lib` with a bundled Noto Sans Lao WOFF that jsPDF can register directly. Do NOT re-enable `foreignObjectRendering: true` without first self-hosting the Lao webfont so it's same-origin.

- **exportCoverPageAsPdf hardened against hang-leaves-UI-broken (2026-07-07)**: Follow-up to the foreignObject hang — the user reported that after the hang, the cover page stayed visible on `/patients` with the wrapper hidden and no way to escape except a page reload. Cause: all the DOM state changes (`wrapper.style.display='none'`, `.print-container` display flips, `target.classList.add('print-active')`, and `await document.fonts.ready`) happened *before* the try/catch, so any hang or throw left the DOM stuck. Refactored [src/main.js:9598](src/main.js#L9598) `window.exportCoverPageAsPdf` so **all state mutations live inside the try block** and the `finally` always restores wrapper display, container displays, `.print-active` class, and inline page styles. Also raced `document.fonts.ready` against a 3-second timeout so a stalled webfont fetch can never wedge the export. Applies only to the cover export; the OPD Card and dashboard html2canvas exports have their own state managers.

## 2026-07-15 — OPD Test view (EMR redesign prototype, NOT committed)

New throwaway view `opd_test` to evaluate a redesigned EMR layout (Epic-style: patient banner + 5 chart tabs + queue rail left + context rail right + sticky action bar). Static demo data only, no DB.

- View: `public/partials/views/opd_test.html` (`#view-opd_test`)
- CSS: `.emrt-*` block appended at end of `src/style.css`
- main.js wiring: loadPartials views array, HIS_NAV_ROUTES `opd_test` -> `/opd/test`, HIS_PATH_ROUTES, loadView views list, `canUserAccessView` (needs `opd` perm), `window.opdTestSwitchTab(el)` at end of file
- Navbar: `#nav-opd_test` ("OPD Test", class `mnu-opd`)
- Full doc: `docs/OPD_TEST_EMR_REDESIGN.md`. Delete these wiring points to remove the prototype.

### 2026-07-15 (v2) — opd_test now single-patient Epic-storyboard layout
Queue/context rails removed; left navy storyboard (patient ID + allergy + encounter + vitals + today timeline) + 5 activity tabs. Only `opd_test.html` + `.emrt-*` CSS block replaced; main.js/navbar wiring unchanged. Still uncommitted prototype.

### 2026-07-15 (v3) — opd_test restyled to house Bootstrap idiom
Navy storyboard dropped ("too AI"). Now: standard page header + col-lg-3 patient `card card-outline card-primary` + col-lg-9 `card card-outline card-info` with Bootstrap nav-tabs and card-footer action bar. Custom CSS reduced to ~20 lines scoped `#view-opd_test`. `opdTestSwitchTab` now toggles `.nav-tabs .nav-link.active`. Still uncommitted.

### 2026-07-16 (v4) — opd_test decluttered (less data on screen)
User feedback: too much unnecessary data displayed. Left patient column removed entirely (encounter no./date/dept/payer/doctor rows + today-timeline gone); "ພາບລວມ" overview tab removed (its 4 cards duplicated SOAP/Lab/History content). New layout: one full-width `card card-outline card-info` with a slim patient banner (avatar 44px, name, sex/age/HN/doctor line, red allergy badge) + inline vitals strip (`.emrt-vs`: BP/Temp/Pulse/RR/SpO₂/weight) in the card-header above 4 tabs (SOAP default · Rx · Lab · ປະຫວັດ). Also dropped: English name, DOB, VN, phone, regular-meds card, procedures empty card, attachments/notes cards, autosave caption, lab Priority + reference-range columns. `.emrt-krow`/`.emrt-tl` CSS deleted, `.emrt-vs` added; `opdTestSwitchTab` unchanged. Still uncommitted prototype.

### 2026-07-16 (v5) — opd_test: left patient card restored, workspace stays lean
User feedback on v4: patient info + vitals must display the old way. Restored the v3 col-lg-3 left card (photo 72px, Lao+English name, sex/age/DOB, HN/VN badges, phone, red allergy alert, Encounter key-value rows, full Vitals rows incl. BMI + Pain Score) — but NOT the "ຂັ້ນຕອນມື້ນີ້" timeline. Right col-lg-9 keeps the v4 declutter: 4 tabs (SOAP default · Rx · Lab · ປະຫວັດ), no overview tab, trimmed lab tables, no autosave caption. CSS: `.emrt-krow` restored, `.emrt-vs` removed, photo back to 72px. main.js/navbar unchanged. Still uncommitted.

## 2026-07-16 — Patient age under 1 year now shows months/days

Infants (e.g. LXH2026-002978, DOB 2025-08-20) showed age "-" in the registration table and "0 ປີ" in the view-detail modal. Added `window.formatAgeFromDob(dob, fallbackAgeYears)` in src/main.js next to `ageFromDob`: ≥1y → "X ປີ", 1–11mo → "X ເດືອນ", <1mo → "X ວັນ"; prefers DOB over the stale DB `Age` integer; falls back to `Age` only when DOB is missing/invalid; future DOB → null → "-". New i18n keys `patients.monthUnit`/`patients.dayUnit` (lo + en). Wired into the registration table age column (replaced the inline year calc) and `viewPatientDetail` `#view_p_age`. Other year-only spots (OPD queue, EMR header, patient dropdown) intentionally unchanged. Node-tested edge cases; doc: docs/PATIENT_AGE_DISPLAY.md.

## 2026-07-16 — Dashboard province chart: ນະຄອນຫຼວງວຽງຈັນ split out + district/province consistency

The province breakdown chart bucketed Vientiane Capital patients under "ວຽງຈັນ" (the ວຽງຈັນ bucket carried a ນະຄອນຫຼວງ alias, and real data contains the misspelling "ມະຄອນຫຼວງວຽງຈັນ" whose "ວຽງຈັນ" substring matched first). Reworked `window.renderProvinceDistrictTables` in src/main.js: new bucket ນະຄອນຫຼວງວຽງຈັນ (matched BEFORE ວຽງຈັນ; aliases ຄອນຫຼວງ/ຄອນຫລວງ cover ນະຄອນ+ມະຄອນ typo variants without catching ຫຼວງພະບາງ; display order ວຽງຈັນ → ບໍລິຄຳໄຊ → ນະຄອນຫຼວງວຽງຈັນ → ອື່ນໆ), and since all five named districts are capital districts, a district hit now forces the province count to ນະຄອນຫຼວງວຽງຈັນ so the two charts agree even on inconsistent/blank Province text. Node-tested; doc: docs/DASHBOARD_PROVINCE_DISTRICT_CHART.md.

### 2026-07-16 (v2) — age format applied system-wide (triage, OPD, report, timeline, IPD)

Follow-up to the infant age fix: every age display now goes through `formatAgeFromDob` (months/days under 1 year). Changed in src/main.js: `buildPatientVisitSummaryData` `age` field is now unit text (feeds report table, visit-history table, Excel/PDF exports; report-detail modal stopped appending " ປີ"); triage + OPD queue builders gained an `ageText` field used by the triage queue badge, the "ບັນທຶກການຊັກປະຫວັດ" modal, the OPD queue detail modal, and the EMR OPD header; OPD Card print `printAge`, QR sticker `dobText`, patient timeline `#timeline_p_info`, and IPD recent-admissions `ageSex` all call the helper. Numeric `age` on queue objects kept (verified nothing writes it to DB); registration form `#ageInput` intentionally still numeric years. Doc: docs/PATIENT_AGE_DISPLAY.md (v2 section).

### 2026-07-16 (v6) — opd_test: action buttons moved to page header, footer removed
User feedback: menus belong at the top; drop what isn't needed yet. Deleted the card-footer action bar entirely; Save Draft · ສົ່ງ Lab · ສົ່ງໃບຢາ · ລົງລາຍເຊັນ/Lock now sit in the page-header row next to the In Progress badge and ກັບຄິວ OPD button. Removed for now: ພິມ OPD Card button and the ນັດຕິດຕາມ date button/label. CSS/main.js/navbar untouched. Still uncommitted prototype.

### 2026-07-16 (v7) — opd_test upgraded to enterprise clinical EMR workflow
Per user spec (Epic/Cerner-style, keep theme/patient panel/header buttons/encounter): tabs replaced with Clinical Note · Orders · Medication · Documents · Timeline · History. Clinical Note = 9 native-<details> accordion sections (CC with duration/severity/onset, HPI, PMH checkboxes, Family Hx, Social Hx, collapsible 8-system ROS, 9-region structured PE grid with WNL toggles, multi-ICD10 Assessment, split Plan). Orders = unified order center (lab/radiology/procedure/referral multi-select, one save), LIS status-chain badges (Ordered…Released/Cancelled), structured lab-result table (parameter/result/unit/ref/flag/comment + verified-by + PDF link). Medication = current meds + new Rx + allergy/interaction/duplicate warning alerts. Documents/Timeline/History per spec. CSS: added .emrt-sec/.emrt-ros/.emrt-pe*/.emrt-lbl/.emrt-check, re-added .emrt-tl, .emrt-sticky patient card (≥992px). New docs/sql/opd_emr_clinical_tables.draft.sql = additive 12-table DDL draft (NOT applied; move into supabase/migrations after layout approval; existing Visits JSON fields remain fallback). main.js/navbar untouched (opdTestSwitchTab is generic; <details> needs no JS). No React/LIS exists in this stack — noted as interpretation in docs. Still uncommitted prototype.

## 2026-07-17 — Patient sticker: visit date/time row restored, fonts enlarged

Regression I introduced: commit 64372f6 ("ship pending work") unintentionally carried sticker changes that deleted the ວັນທີມາກວດ row (printDate1-3/printTime1-3) from print-areas.html and removed the pre-print date/time chooser (getStickerDateTimeDefaults, formatStickerPrintDate) from printQRCard in src/main.js. Restored all of it from 70514f3 (last correct version) — dialog defaults to now, date shown as dd/mm/yyyy. Also enlarged sticker text per user request: .pcard-label 26→28px, .pcard-value/.pcard-time 30→33px, .pcard-name 32→35px, and tightened .pcard-info gap 2.2→1.8mm so long two-line names still fit the 89mm card. .pcard-id stays 44px. Doc: docs/PATIENT_STICKER.md. Not yet committed — awaiting user print-preview test.

### 2026-07-17 (v2) — sticker font enlargement reverted per user
User said "ແກ້ກັບຄືນ" after the restore: keep the sticker exactly as the last-good version, no font changes. Reverted .pcard-label/.pcard-value/.pcard-time/.pcard-name and .pcard-info gap to 26/30/30/32px + 2.2mm — the pcard CSS block, sticker markup in print-areas.html, and printQRCard now match commit 70514f3 byte-for-byte except the intentional formatAgeFromDob age line (infants show months/days). Verified with git diff against 70514f3. Awaiting user print-preview test before commit.
